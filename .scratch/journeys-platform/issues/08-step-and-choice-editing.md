# 08: Step and Choice editing

Status: ready-for-human
Blocked by: 03
Owner: Atlas orchestrator (Claude Fable 5.1), session of Paul Macfarlane, claimed 2026-09-20
Parent: `.scratch/journeys-platform/spec.md`

**What to build:** An Author edits a Journey's Draft without a canvas: a step list plus a side panel where they edit a Step's title and rich-text content (Tiptap; headings, bold, italic, lists, links, image by URL with required credit), add/reorder/retarget/delete Choices, create the target Step in the same motion as a Choice, designate the Start, manage the Journey's Outcomes, assign an Outcome to an Ending, delete a Step and see which Choices broke, and run validation on demand to see the problem list. Optional Prompt editing arrives in ticket 12. Draft autosaves; last write wins.

- [ ] Author builds a 6-step branching journey with two Endings and two Outcomes entirely through the panel; it publishes.
- [ ] Deleting a targeted Step shows the affected Choices; validation then lists the broken targets.
- [ ] Image insertion refuses to save without a credit; content saved is Tiptap JSON and renders identically in Preview.
- [ ] Choice reorder and retarget persist; "create new Step from this Choice" opens the new Step in the panel.
- [ ] Outcome rename keeps its id (Seam A) and the Ending's assignment.
- [ ] Seam B: build → validate (see a problem) → fix → publish.

Verification and evidence follow `docs/agents/testing.md`: cite the exact commands run; commit any artifact used as PASS evidence under `test-results/`; never include participant Responses or real run data. Use `CONTEXT.md` vocabulary. Spec: `.scratch/journeys-platform/spec.md`.

## Comments

### [EXECUTION PLAN] 2026-09-20 — Atlas orchestrator

**Contract:** this ticket as it stands (no scope changes on file). Criteria are AC-1..AC-6 in checklist order. Derived DoD from the footer, `docs/agents/testing.md`, and `CLAUDE.md`: DoD-1 verified run command green; DoD-2 every PASS artifact committed under `test-results/`, fixture journeys only, no participant data; DoD-3 the one new dependency is recorded in `package.json`, `pnpm-lock.yaml` is updated locally and committed by Paul from his own terminal (the Atlas plugin's commit-time secret scrub refuses lockfile hashes in agent commits), and the PR's CI install passes. Run surface: local + deployed; per Paul's 2026-09-20 decision there is no post-merge staging smoke gate, so no deployed criterion is defined. Prompt editing is ticket 12 and the canvas is ticket 09; neither is built here.

**Availability:** tickets 06 and 07 are blocked (05 is `ready-for-human`, not `done`); 08 and 13 are available; 08 is first by number and unblocks 07, 09, 12, and 14.

**Repository delivery:** `journeys`, base `staging` @ `6e36ede`, branch `feat/08-step-and-choice-editing`, direct checkout, no worktrees. Two deliverables run **sequentially** (D1 then D2) because D2 imports D1's module and both verify through the one e2e port (3100) and the one `journeys_e2e` database; no file conflict is predicted (D1 owns `src/lib/graph/edit.ts`, `src/lib/rich-text/extensions.ts`, `package.json`; D2 owns `src/components/journeys/`, the Journey page, `e2e/`, README). Re-checked at closeout.

**Resolved technical decisions (execution, not contract changes):**

- **Dependency.** `pnpm add @tiptap/react@3.31.3` (the installed Tiptap version). `package.json` is committed by the agent; `pnpm-lock.yaml` is Paul's commit (human gate HG-1).
- **Editor extension set.** `src/lib/rich-text/extensions.ts` gains `editorExtensions`: the same StarterKit configuration as `richTextExtensions` with `undoRedo`, `dropcursor`, and `gapcursor` re-enabled (they emit no content, so the closed content set is unchanged) plus `CreditedImage`. `richTextExtensions` stays as it is for the renderer.
- **Pure edit module** `src/lib/graph/edit.ts` (no `server-only`; every function returns a new document and never mutates its input; new ids come from `crypto.randomUUID()`; a mutation naming a missing Step, Choice, or Outcome returns the document unchanged): `addStep(doc, title?)` → `{ document, stepId }` (title default "Untitled step", empty-paragraph content, no Choices, `prompt: null`, `outcomeId: null`, `position: null`); `updateStep(doc, stepId, patch: Partial<Pick<Step, "title" | "content" | "outcomeId">>)`; `setStart(doc, stepId)`; `choicesTargeting(doc, stepId)` → `Array<{ stepId, stepTitle, choice }>` for Choices on other Steps aimed at it; `deleteStep(doc, stepId)` → `{ ok: true, document, brokenChoices }` (the Step is removed, Choices aimed at it stay in place and dangle) or `{ ok: false, error }` for the Start; `addChoice(doc, stepId, { label, targetStepId })` → `{ document, choiceId }`; `addChoiceToNewStep(doc, stepId, { label, title? })` → `{ document, choiceId, stepId }`; `updateChoice(doc, stepId, choiceId, patch: Partial<Pick<Choice, "label" | "targetStepId">>)`; `moveChoice(doc, stepId, choiceId, "up" | "down")` (no-op at the ends); `removeChoice(doc, stepId, choiceId)`; `retargetChoiceToNewStep(doc, stepId, choiceId, title?)` → `{ document, stepId }`; `addOutcome(doc, label)` → `{ document, outcomeId }`; `renameOutcome(doc, outcomeId, label)` (id and every Ending's assignment unchanged); `removeOutcome(doc, outcomeId)` → `{ ok: true, document } | { ok: false, error }` (refused while any Ending is tagged with it); `endingCountsByOutcome(doc)` → `Record<string, number>`; `stepName(step)` (title, or the id when blank). Seam A tests per function, including: rename keeps the id and the Ending's tag (AC-5); delete reports the broken Choices and `validateForPublish` then reports one `dangling-choice-target` per broken Choice (AC-2); moved Choice order is the new order; `removeOutcome` refused in use; `addChoiceToNewStep` yields a Step reachable from the Start; the Start cannot be deleted.
- **Editor UI** `src/components/journeys/draft-editor.tsx` (`"use client"`) replaces `DraftSummary` on the Journey page (`draft-summary.tsx` is deleted); the page container widens from `max-w-3xl` to `max-w-6xl`. Header row: heading "Steps", the summary "N steps · M outcomes", a save status (`role="status"`: "Saved" / "Saving…" / "Unsaved changes"), and buttons "Add step" and "Validate". Below, two columns at `lg`: left the Outcomes section then the step list; right the step panel.
  - **State and autosave.** `document` lives in React state, initialized from the `draft` prop; a `lastSaved` ref holds the document as last persisted (initially the prop). When the `draft` prop changes on a server refresh and `!documentsEqual(draft, lastSaved)` — a restore or another Member's write — the editor adopts it (last write wins) and re-selects the Start if the selected Step is gone; when it equals `lastSaved` the change is our own save coming back and is ignored. Every mutation sets state and schedules `saveDraftAction(projectId, journeyId, document)` after 600 ms of quiet; an edit during a save queues exactly one more; the pending save is flushed on `focusout` leaving the editor root, on `beforeunload` (best effort, with the unsaved-changes warning), and before Validate. On `{ ok: true }`: `lastSaved` = the saved document, status "Saved", and `router.refresh()` when nothing further is pending so the Publish button's enabled state follows the Draft. On `{ ok: false }`: `role="alert"` "Couldn't save: <error>", the Step named by `stepId` is selected, editing continues and the next edit retries.
  - **Rich text.** `useEditor({ extensions: editorExtensions, content, immediatelyRender: false, onUpdate })`; the ProseMirror element carries `aria-label="Step content"` through `editorProps.attributes`. On update the editor's JSON goes through `sanitizeContent` before it enters `document`, so client state is the stored shape and `documentsEqual` compares like with like; a refused sanitize shows its error and leaves the document as it was. Switching Steps calls `setContent(step.content, { emitUpdate: false })`. Toolbar buttons with `aria-label` "Heading 1", "Heading 2", "Heading 3", "Bold", "Italic", "Bullet list", "Numbered list", "Link", "Image", each with `aria-pressed` from `editor.isActive`. Link: `Dialog` "Add link" with a "URL" input that must begin with `http://` or `https://` (inline error otherwise) and "Apply link" (an empty URL removes the link). Image: `Dialog` "Add image" with "Image URL" and "Credit" inputs; "Insert image" is refused with the inline error "Every image needs a credit" while the credit is blank (the server's sanitizer refuses the same document, so the rule holds twice); on insert `setImage({ src, credit })`.
  - **Step list.** `<ul role="list" aria-label="Steps">`, one `<li>` per Step in document order holding a `<button>` with the title (`stepName`), `aria-current="true"` on the selected Step, `Badge` "Start" / "Ending", and "N choices". The Start is selected on load.
  - **Step panel** (`<section aria-label="Step">`, heading = the Step's title): `Input` labelled "Step title" (`maxLength` 200); "Make this the start" (hidden on the Start); the rich text editor; the Choices section; for an Ending a `<select aria-label="Outcome">` with "No outcome" plus every Outcome; "Delete step".
  - **Choices.** `<ul role="list" aria-label="Choices">`, one row per Choice: `Input aria-label="Choice label"`, native `<select aria-label="Choice target">` listing every other Step by name plus a final option "New step…" (choosing it creates a Step, retargets this Choice to it, and opens it in the panel — AC-4), buttons "Move up" / "Move down" (disabled at the ends) and "Remove choice". A dangling target shows a selected disabled option "Missing step" and the text "This choice points at a step that no longer exists". "Add choice" reveals an inline form: `Input` labelled "Label", `<select>` labelled "Target" (every other Step plus "New step", default "New step") and "Add" — with "New step" the Step is created and opened in the panel (the same-motion rule); the form closes after adding.
  - **Delete step.** `AlertDialog` "Delete this step?" listing, from `choicesTargeting`, each Choice that will break as "<label or 'Untitled choice'> on <step name>", or "No choices point at this step."; confirm "Delete step". Disabled on the Start with the hint "Make another step the start first". After deletion the Start is selected and the broken Choices show "Missing step".
  - **Outcomes.** `<section aria-label="Outcomes">` heading "Outcomes": `<ul role="list" aria-label="Outcomes">` rows with `Input aria-label="Outcome label"` (rename in place; the id never changes), "N endings", and "Remove outcome" (disabled with the hint "Endings still use this outcome" while any does); below, an `Input` labelled "New outcome" and "Add outcome".
  - **Validate.** Flush the pending save, then `validateDraftAction`; render `<section aria-label="Validation">` with "No problems found." or `<ul role="list" aria-label="Validation problems">`, each message a `<button>` when it carries a `stepId` (selects that Step) and plain text otherwise.
  - Author-facing copy uses `CONTEXT.md` words only (Step, Choice, Start, Ending, Outcome, Draft).
- **Seam B** `e2e/step-editing.spec.ts`, one test per criterion (names are the evidence directories); every Draft is built through the panel, never written into the row: `step-editing-build-and-publish` (AC-1, AC-6: reach 6 Steps with 2 Endings and 2 Outcomes; Validate first shows the "has no outcome" problems; assign Outcomes; Validate shows "No problems found."; Publish; badge "Published"; the `draft` row read back holds 6 Steps and 2 Outcomes); `step-editing-delete-and-validate` (AC-2: delete a targeted Step; the dialog names the Choice; the row shows "Missing step"; Validate lists the dangling target); `step-editing-image-credit-and-preview` (AC-3: "Insert image" without a credit is refused and the row stays image-free; with a credit it saves; heading, bold, and a list are typed; Preview's Start Step renders `figure > img` with that `src`, a `figcaption` with the credit, `h2`, `strong`, and `li`; the row's content is a `doc` whose image block carries the credit); `step-editing-choices-reorder-retarget` (AC-4: "Move up" order survives a reload; a retarget survives a reload; "New step…" opens the new Step in the panel); `step-editing-outcome-rename` (AC-5: the outcome id in the row is unchanged after renaming and the Ending is still tagged; Preview's Ending shows the new label). The existing `journey-draft` and `publish-*` specs must stay green; selectors change only where the new list requires. Screenshots at `test-results/<test-name>/<test-name>.png`.
- README: the journey page entry gains the step editor; no new route.

**Deliverables:**
- **D1 — edit module, editor extension set, dependency** (worker: atlas-worker on `sonnet`, tightly specified): `pnpm add @tiptap/react@3.31.3`, `editorExtensions`, `src/lib/graph/edit.ts` + `edit.test.ts`.
- **D2 — the editor on the Journey page, with Seam B** (worker: atlas-worker on `opus`): `draft-editor.tsx` and its parts, page integration, `e2e/step-editing.spec.ts`, README. Depends on D1.

**Verification map (evidence committed under `test-results/`, root cleared at workspace prep):**

| Criterion | Command / action | Surface & real deps | Expected | Evidence | Earliest | Invalidated by |
|---|---|---|---|---|---|---|
| AC-1 | `pnpm test:e2e` test `step-editing-build-and-publish` | local; docker Postgres `journeys_e2e`; e2e server on 3100 | 6 Steps, 2 Endings, 2 Outcomes built through the panel; Publish succeeds; badge "Published" | `test-results/step-editing-build-and-publish/step-editing-build-and-publish.png`, `test-results/dod-1-e2e.txt` | after D2 | changes under `src/`, `e2e/` |
| AC-2 | `pnpm test:e2e` test `step-editing-delete-and-validate`; Seam A `edit.test.ts` delete cases | as AC-1; Vitest | dialog names the affected Choice; "Missing step" shown; Validate lists the dangling target | `test-results/step-editing-delete-and-validate/…png`, `test-results/dod-1-e2e.txt`, `test-results/dod-1-commands.txt` (unit line) | after D2 (Seam A after D1) | as AC-1 |
| AC-3 | `pnpm test:e2e` test `step-editing-image-credit-and-preview` | as AC-1 | credit-less insert refused; content in the row is Tiptap JSON; Preview renders the same heading, bold, list, image and credit | `test-results/step-editing-image-credit-and-preview/…png`, `test-results/dod-1-e2e.txt` | after D2 | as AC-1 |
| AC-4 | `pnpm test:e2e` test `step-editing-choices-reorder-retarget`; Seam A `moveChoice`/`updateChoice`/`retargetChoiceToNewStep` | as AC-1; Vitest | order and target persist across reload; "New step…" opens the new Step | `test-results/step-editing-choices-reorder-retarget/…png`, `test-results/dod-1-e2e.txt` | after D2 | as AC-1 |
| AC-5 | `pnpm test:e2e` test `step-editing-outcome-rename`; Seam A `renameOutcome` | as AC-1; Vitest | outcome id unchanged; Ending still tagged; Preview shows the new label | `test-results/step-editing-outcome-rename/…png`, `test-results/dod-1-e2e.txt` | after D2 | as AC-1 |
| AC-6 | the `step-editing-build-and-publish` flow: build → Validate (problem) → fix → Validate (clean) → Publish | as AC-1 | as stated | as AC-1 | after D2 | as AC-1 |
| DoD-1 | `DATABASE_URL=postgresql://postgres:postgres@localhost:5436/journeys pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build && DATABASE_URL=postgresql://postgres:postgres@localhost:5436/journeys pnpm test:e2e` | local; docker Postgres | all exit 0 | `test-results/dod-1-commands.txt`, `test-results/dod-1-e2e.txt` | after D2 | any source, config, or test change |
| DoD-2 | evidence review: every PASS artifact committed under `test-results/`; fixture journeys only | local | as stated | this ticket's closeout | closeout | — |
| DoD-3 | `git diff staging -- package.json` shows only `@tiptap/react`; **HG-1** Paul commits `pnpm-lock.yaml` on the branch; post-check `gh pr checks` install/CI green after the push | local + GitHub CI | lockfile committed by a human; CI install passes | this ticket's closeout, the PR's checks | closeout | any dependency change |

**Human gates:** HG-1 (DoD-3) — prerequisite: D1 has run `pnpm add` on the branch; action: Paul runs `git add pnpm-lock.yaml && git commit -m "chore: commit pnpm-lock.yaml"` from his own terminal on `feat/08-step-and-choice-editing`; expected: the lockfile commit is on the branch before or right after the PR opens, and CI's install step passes. Announced now, raised at closeout. No other gate: the Migrate action has nothing to apply (no migration in this ticket).

### [PROGRESS] 2026-09-21 — D1 and D2 integrated; aggregate review started

D1 (pure edit operations `src/lib/graph/edit.ts` with 35 Seam A cases, `editorExtensions`, `@tiptap/react@3.31.3` added) — atlas-worker on `sonnet` — `6624932`, accepted after the orchestrator's screen; unit suite rerun by the orchestrator, 146 passed. D2 (the editor on the Journey page: `draft-editor.tsx` with `step-list`, `step-panel`, `choice-list`, `outcome-list`, `rich-text-editor`; `DraftSummary` removed; `e2e/step-editing.spec.ts` with the five planned tests; README) — atlas-worker on `opus` — `5cabfec`, accepted. The worker had to add `{ exact: true }` to two `getByLabel("Title")` calls in `e2e/projects-and-journeys.spec.ts` because the panel's "Step title" now also matches, and set a 180 s timeout on the new spec file; it also caught and fixed a duplicate-key bug (two siblings keyed `step.id`) that Playwright exposed. Candidate evidence: the worker's verified run command all exit 0 at `5cabfec` (146 unit tests; 24 e2e specs, `[e2e] database: journeys_e2e on localhost:5436`) with the five screenshots present in the checkout, not yet committed. Carried into the review: the rich text surface resets only when the selected Step changes, so a Restore on the same page leaves the editor showing the pre-restore text for a still-selected Step. Status `in-progress` → `ai-review`. `pnpm-lock.yaml` is modified and uncommitted (HG-1).

### [AI CODE REVIEW] 2026-09-21 — aggregate review of `6e36ede..5cabfec`, fixes in `17b2196`

Two fresh reviewers (one per axis, `opus`, read-only) read the whole diff against the fixed base with the ticket, its `[EXECUTION PLAN]`, the spec's Steps / Choices / Outcomes / Validation / Graph document / Testing Decisions sections, `CONTEXT.md`, ADR-0001, and `docs/agents/testing.md`; the orchestrator adjudicated every candidate from the cited hunks and applied the fixes inline in `17b2196`. **One blocking finding (T1), resolved; no unresolved blocking finding on either axis.**

**Axis 1 — technical implementation and spec conformity** (7 candidates)

| # | Severity | Paths | Disposition |
|---|---|---|---|
| T1 the rich text surface was re-fed only when the selected Step changed, so a Restore (or another Member's write) under the same Step left the pre-restore text on screen and the next keystroke wrote it back | **blocking** | `rich-text-editor.tsx`, `draft-editor.tsx`, `step-panel.tsx` | resolved: the editor counts external adoptions in a `revision` and the surface resets on `${stepId}:${revision}`; `publish-versions-and-restore` now edits the Start's text as well as its title and asserts the surface shows version 1's words after Restore |
| T2 a pasted or dropped image has no credit, so the sanitizer refused that update and every one after it while the status still read "Saved" — everything typed into the Step was thrown away | non-blocking (data loss) | `rich-text-editor.tsx`, `draft-editor.tsx` | resolved: the credit-less image is removed from the surface, the update that follows carries the rest, and a notice ("Every image needs a credit, so the pasted image was removed. Use Image to add one with its credit.") stays while that Step is selected |
| T3 the status could read "Saved" for up to 600 ms while an edit made mid-save waited for its timer, a flake source for the specs that gate on it | non-blocking | `draft-editor.tsx` | resolved: "saved" and the refresh only when the document equals what was stored |
| T4 a server render started before the latest save could arrive after it and be adopted over newer local edits | non-blocking | `draft-editor.tsx` | resolved: nothing is adopted while an edit is unsaved or a save is running; the save about to happen wins, as last write does |
| T5 an Outcome became unremovable once its Ending gained a Choice (the tag survives, the Outcome select only renders on Endings, and the plan's rule said "any Ending") | non-blocking | `edit.ts`, `outcome-list.tsx` | resolved: the rule is Endings only in both places and removal clears the stale tag from non-Ending Steps; Seam A case added |
| T6 the dialogs' URL test (`/^https?:\/\//`) was looser than the sanitizer's (`new URL()`), so `https://` alone showed an image the Draft never stored | non-blocking | `rich-text-editor.tsx` | resolved: same `new URL()` rule |
| T7 (a) `insertContent` instead of the plan's `setImage({ src, credit })`; (b) `@tiptap/react` pinned exactly; (c) Cancel buttons the plan did not list | non-blocking | `rich-text-editor.tsx`, `package.json`, `choice-list.tsx` | (a) and (c) deviation approved (mechanical: `setImage`'s typed options lack `credit`; the app's other dialogs have Cancel); (b) resolved: `^3.31.3`, lockfile specifier resynced locally for Paul's commit |

Conformity: AC-1..AC-6 conform (AC-3 with T2 and T6 applied); DoD-1 and DoD-2 rest on the evidence captured at aggregate verification; DoD-3 conforms for `package.json` with T7b applied and waits on HG-1 for the lockfile.

**Axis 2 — coding standards** (10 candidates)

| # | Severity | Paths | Disposition |
|---|---|---|---|
| S1 comments in `actions.ts`, `publish.spec.ts`, `draft.spec.ts`, `documents.ts` still said the editor had not arrived | non-blocking | those four files | resolved |
| S2 `"use client"` on the five components only `draft-editor.tsx` imports made each a client entry (Next's plugin warned about their function props); the repo's convention puts the directive only on files a server component imports | non-blocking | `choice-list`, `outcome-list`, `step-list`, `step-panel`, `rich-text-editor` | resolved: directive removed from the five |
| S3 `@tiptap/react` exact pin | non-blocking | `package.json` | resolved (T7b) |
| S4 `stepName`/`hasStep`/`hasOutcome` duplicated between `edit.ts` and `validate.ts` | non-blocking | `edit.ts`, `validate.ts`, `document.ts` | resolved: lifted into `document.ts`, both import them; `edit.ts` re-exports `stepName` for the components |
| S5 `as const` on the shared StarterKit options "buys nothing"; asymmetric spread | non-blocking | `extensions.ts` | partly rejected: the assertion is required (StarterKit's options take `false \| object`, so a widened `boolean` fails typecheck); the spread is now symmetric |
| S6 `dod-1-*.txt` absent and nothing under `test-results/` committed | non-blocking until closeout | `test-results/` | resolved: captured at `17b2196`, committed in `a0f7277` |
| S7 two new `role="list"` lists lacked the rationale comment | non-blocking | `draft-editor.tsx`, `step-panel.tsx` | resolved |
| S8 `document` shadows the global in five browser components | non-blocking | the five components | deviation approved: the name mirrors the domain noun `edit.ts` uses and none of the files touch the DOM; recorded as a remaining risk |
| S9 `SELECT_CLASS` claimed to match `Input` but differed in padding, type size, and transition | non-blocking | `editor-shared.ts` | resolved: `Input`'s tokens |
| S10 `insertContent` deviation | non-blocking | `rich-text-editor.tsx` | deviation approved (T7a) |

Both axes confirmed: no new file under `src/lib/` or `src/components/` imports a database module; every `edit.ts` operation is pure and ids come from `crypto.randomUUID()`; Outcome rename never moves an id; `Object.hasOwn` at every id lookup with a Seam A `"toString"` case; base-ui `render` usage matches the existing dialogs; the five test names and screenshot paths match the plan exactly; fixture Authors and invented Journeys only; `pnpm-lock.yaml` is in no agent commit.

**Remaining risks (not findings):** `router.refresh()` after every successful save makes the build spec drive many RSC re-renders against `next dev`, which the spec file's 180 s timeout absorbs; the components read the rendered `document` prop while the rich text path reads the ref, which agree only because every mutation commits a render before the next event; neither seam drives "Make this the start" or the Link dialog through the browser (Seam A covers `setStart`); the validation list persists after the problems are fixed until Validate is clicked again; the Publish button can be clicked within the 600 ms debounce window after an edit (focus leaving the editor flushes the save first, so the write normally lands ahead of the publish, but the two are not ordered).

### [CLOSEOUT] 2026-09-21 — Atlas orchestrator

**PR:** https://github.com/paul-macfarlane/journeys/pull/16 (base `staging`, head `feat/08-step-and-choice-editing`). Status `in-progress` → `ai-review` → `ready-for-human`.

**Repository delivery `journeys`:** base `staging` @ `6e36ede`, direct checkout, no worktrees; D1 then D2 sequentially. Closeout re-check of the isolation record: D1 (`6624932`) touched `package.json`, `src/lib/graph/edit.ts`, `edit.test.ts`, and `src/lib/rich-text/extensions.ts`; D2 (`5cabfec`) touched none of those, so the no-conflict prediction held. The serialization rested on D2 importing D1's module and on the single e2e port and database, both of which also held; the record stands as written.

**Deliverables:**
- D1 pure edit operations, editor extension set, `@tiptap/react` — atlas-worker on `sonnet` — `6624932`.
- D2 the editor on the Journey page with Seam B — atlas-worker on `opus` — `5cabfec`.
- Review fixes — orchestrator — `17b2196`. Evidence — orchestrator — `a0f7277`. This closeout record follows.

**Verified run command:** `DATABASE_URL=postgresql://postgres:postgres@localhost:5436/journeys pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build && DATABASE_URL=postgresql://postgres:postgres@localhost:5436/journeys pnpm test:e2e` — every command exit 0 at `17b2196` (147 unit tests in 7 files; 24 e2e specs, `[e2e] database: journeys_e2e on localhost:5436`). No deployed-target check (Paul's 2026-09-20 decision); no migration in this ticket.

**Criterion verdicts (evidence under `test-results/`, committed in `a0f7277`, captured at `17b2196`):**

| Criterion | Verdict | Evidence |
|---|---|---|
| AC-1 six Steps, two Endings, two Outcomes built through the panel; publishes | PASS | `step-editing-build-and-publish/step-editing-build-and-publish.png`, `dod-1-e2e.txt` (`step-editing-build-and-publish` ✓; the row read back holds 6 Steps and 2 Outcomes) |
| AC-2 deleting a targeted Step shows the affected Choices; validation lists the broken targets | PASS | `step-editing-delete-and-validate/…png`, `dod-1-e2e.txt` ✓, `dod-1-commands.txt` (Seam A `deleteStep` cases) |
| AC-3 credit-less image refused; content is Tiptap JSON; renders identically in Preview | PASS | `step-editing-image-credit-and-preview/…png` (Preview showing the heading, bold, list, image and credit), `dod-1-e2e.txt` ✓ |
| AC-4 reorder and retarget persist; "New step…" opens the new Step in the panel | PASS | `step-editing-choices-reorder-retarget/…png`, `dod-1-e2e.txt` ✓ (order and target re-read after reload) |
| AC-5 Outcome rename keeps its id and the Ending's tag | PASS | `step-editing-outcome-rename/…png`, `dod-1-e2e.txt` ✓ (id compared before and after in the row), `dod-1-commands.txt` (Seam A `renameOutcome`) |
| AC-6 build → validate (problem) → fix → publish | PASS | as AC-1 (Validate first lists two "has no outcome" problems, then "No problems found.", then Publish) |
| DoD-1 verified run command green | PASS | `dod-1-commands.txt`, `dod-1-e2e.txt` (eight repeated Next dev console warnings, pre-existing and unrelated, were removed from the e2e capture and the header says so) |
| DoD-2 every PASS artifact committed; fixture data only | PASS | this record; the artifacts hold minted `Test Author` accounts, invented Border-post Journeys, a placeholder image URL, and nothing from a participant |
| DoD-3 dependency recorded; lockfile committed by Paul; CI install passes | BLOCKED on HG-1 | `package.json` differs from `staging` by the one `@tiptap/react` line (verified at `17b2196`); `pnpm-lock.yaml` is modified in the checkout and in no commit; post-check after Paul's commit: `gh pr checks 16` |

**Deviations:** T7a/S10 (`insertContent` for the credited image), T7c (Cancel buttons), S5 (`as const` kept, required), S8 (`document` naming kept) — all orchestrator-approved, non-blocking, recorded above.

**Human follow-ups:** (1) **HG-1 / DoD-3:** from your own terminal on `feat/08-step-and-choice-editing`, `git add pnpm-lock.yaml && git commit -m "chore: commit pnpm-lock.yaml" && git push`; CI cannot install until it lands, and `gh pr checks 16` is the post-check. (2) Review and merge PR #16. (3) Move this ticket to `done` after merging; that unblocks 07 (with 05), 09 (with 04), 12 (with 06), and 14.
