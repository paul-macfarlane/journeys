# 22: Canvas simplification — feedback round 1

Status: ai-review
Blocked by: 21
Owner: Atlas orchestrator (Claude Fable 5.1), session of Paul Macfarlane, claimed 2026-09-21
Parent: `.scratch/journeys-platform/spec.md`
Priority: next after 21 (Paul, 2026-09-21): 21 → 22 → 23 → 17; 20 at any time. "Anything we can do to simplify the canvas and make it less buggy would be good."

**Why:** Paul used the canvas after tickets 16–21 and found it moves the view when he did not ask, shows more than he needs, and fights him on a large map (case-3). This ticket removes what did not earn its place and makes every viewport move one the Author asked for.

**Decisions (Paul, 2026-09-21):** the map never zooms all the way out on its own after the first render — not when the panel comes back for an opened Step, not when a Step is added; it zooms to the Step instead. The box toolbar shows only after the Author clicks a box, and is compact until expanded. The Ending colour bar and the legend go. The Choice target picker is searchable. Undo/redo is ticket 23. "Leads here from" goes. A Choice dragged into empty space makes a new Step. Clicking an arrow never moves focus or scrolls the page away from the canvas. Outcomes stay a concept (analytics in ticket 10 still group Endings by Outcome), but the dedicated Outcomes section beneath the map goes and an Outcome is made, chosen, and renamed from the Ending itself.

**What to build:**

- **The view stays where the Author put it.** The map fits itself only on first render, on a direction change, and on "Hide panel" / "Show panel". Opening a Step that reveals the hidden panel keeps the viewport (the frame narrows; nothing re-fits) and then makes the "Zoom to step" move on the opened Step (`fitView({ nodes: [{ id }], maxZoom: 1, duration: 200 })`). "Add step", "Add next step", "Duplicate", and a Choice dropped on empty map (below) zoom to the new Step the same way; ticket 16's "a Step added or removed fits the whole map" is withdrawn — a removal leaves the view alone too. Ticket 21's fit on Hide/Show stays.
- **A compact box toolbar, only on a click.** The toolbar appears on a box only after the Author clicks that box on the map (not for the Step the panel holds on page load, not for a Step opened from "Find step", a problem entry, or an arrow), as one small button on the box labelled "Step actions" (an ellipsis icon, `aria-expanded`) that expands to the five moves (Add next step, Duplicate, Zoom to step, Make this the start, Delete step); Escape or a click elsewhere collapses it; the panel footer keeps its own Duplicate / Make this the start / Delete step.
- **No Ending colouring.** Remove the Outcome colour bar on Ending boxes, `data-outcome-index`, `OUTCOME_COLORS`, and the legend panel entirely (Start and Problem read from the badges and rings). Ticket 10 may reintroduce colour if analytics need it.
- **Searchable Choice target.** The target `<select>` in a Choice row becomes a combobox in the "Find step" pattern: the field shows the current target's title; clicking or typing opens a listbox of every Step in map order (the current Step included, ticket 18), filtered by title as the Author types; Enter or a click retargets the Choice through `updateChoice`. Same ARIA shape as `find-step.tsx`; extract the shared pieces rather than copying them.
- **The selected arrow is the one you can take hold of.** Every arrow into a box ends on the same handle, so their heads stack. Only the selected arrow is `reconnectable`, and the selected arrow is drawn above the others (React Flow `zIndex` or ordering), so dragging a head always moves the Choice the Author selected.
- **"Leads here from" removed** from the panel.
- **Drop a Choice on empty map to make a Step.** Dragging from a box's connect dot and releasing on bare map creates a new Step and the Choice leading to it in one motion (`addChoiceToNewStep`), opened with the title focused, the map zoomed to it. Until ticket 17 lands the new Step is placed by dagre; 17 may use the drop point.
- **Clicking an arrow stays on the canvas.** Selecting an arrow opens its Step in the panel and marks that Choice's row (`aria-current="true"`, a ring) but moves no focus and scrolls nothing. The same for a Choice drawn by dragging: the Step opens with the new row marked, focus stays on the map; ticket 16's "label field focused" is withdrawn. `SelectStepOptions.focusChoiceId` becomes `markChoiceId`.
- **Outcomes from the Ending.** Remove `OutcomeList` and the "Outcomes" heading beneath the map. The Ending's Outcome field in the panel becomes a combobox: it lists every Outcome with its Ending count; typing a label no Outcome has offers "Create outcome “<label>”", which adds it and tags the Ending; choosing one tags the Ending; "No outcome" clears it. A "Rename" button beside the field turns the label into an inline input that renames the Outcome for every Ending sharing it (`renameOutcome`). An Outcome no Ending uses any longer is removed from the document (`removeOutcome`) when the last Ending drops it — no explicit remove. The "n steps · m outcomes" summary stays; the seed documents, `validateForPublish`, the runner, and analytics are unchanged.
- **Specs and docs.** Every e2e spec that relied on the withdrawn behaviours (fit on add, label focus after a drag, the Outcomes section, "Leads here from", the legend, `data-outcome-index`, the toolbar on the initial Step) is rewritten to the new ones — never skipped. README's editor paragraph and `CONTEXT.md` (Canvas) are updated. No spec `[SCOPE CHANGE]` is needed: the spec never mandated the legend, the Outcomes section's placement, or the viewport rules.

Acceptance criteria:

- [ ] On case-3 zoomed in to a box with the panel hidden, clicking another box on the map shows the panel and leaves the zoom no lower than before, with the clicked box fully on the map; "Add next step" from a box on case-3 zoomed in leaves the zoom no lower than before and the new box fully on the map; deleting a Step leaves the viewport transform unchanged.
- [ ] On page load no toolbar and no "Step actions" button is on the map; clicking a box shows "Step actions" on that box; expanding it shows the five moves; Escape collapses it; opening a Step from "Find step" shows no toolbar.
- [ ] No `[data-outcome-bar]`, no `data-outcome-index`, and no "Legend" list on the map, on case-3 with its three Outcomes.
- [ ] In a Choice row, typing part of another Step's title in the target field lists it; choosing it retargets the Choice; the `draft` row read back holds the new target; the arrow's accessible name names the new target.
- [ ] With two Choices from two Steps into one box, selecting the second arrow and dragging its head onto a third box retargets that Choice and leaves the first unchanged (the `draft` row read back).
- [ ] No "Leads here from" group in the panel.
- [ ] Dragging from a box's connect dot onto empty map creates a Step and a Choice to it; the panel opens on the new Step with its title focused; the new box is fully on the map; the `draft` row holds both.
- [ ] Clicking an arrow opens its Step with that Choice's row marked current; the document's active element stays inside the Canvas region; `window.scrollY` is unchanged.
- [ ] No "Outcomes" section beneath the map. On an Ending, typing "Reached care" in the Outcome field and choosing "Create outcome" tags it and the summary reads "1 outcome"; a second Ending picks it from the list; renaming it renames both (the `draft` row read back); clearing it on both Endings removes it and the summary reads "0 outcomes".
- [ ] Seam B: build a three-Step branch by dropping two Choices on empty map, tag both Endings with one Outcome made from the panel, publish, walk it in the runner. Screenshots and a recording under `test-results/`; every existing canvas, draft, step-editing, and publish spec passes.

Verification and evidence follow `docs/agents/testing.md`: cite the exact commands run; commit any artifact used as PASS evidence under `test-results/`; never include participant Responses or real run data. Use `CONTEXT.md` vocabulary. Spec: `.scratch/journeys-platform/spec.md`. Origin: Paul's feedback after using the canvas, 2026-09-21.

## Comments

### [EXECUTION PLAN] 2026-09-21 — Atlas orchestrator

**Contract:** this ticket as it stands: Paul's decisions, the ten "What to build" bullets, and the ten acceptance criteria (AC-1..AC-10 in checklist order; AC-10 is Seam B). Readings resolved from the ticket's own text, not changes to it: (a) ticket 17 has not landed, so a Step made by a drop on empty map is placed by dagre; (b) "the 'Zoom to step' move" is `fitView({ nodes: [{ id }], maxZoom, duration: 200 })` with `maxZoom` the greater of 1 and the zoom the map is at, because AC-1 also requires the zoom to be no lower than before — at 0.16 (case-3 fitted) it goes to 1, at 1.5 it stays at 1.5 and centres; the box toolbar's own "Zoom to step" keeps its `maxZoom: 1.5`; (c) "deleting a Step leaves the viewport transform unchanged" holds even when the delete reveals a hidden panel: the frame narrows and nothing else moves; (d) "when the last Ending drops it" is the panel's own untag or retag — an Outcome orphaned by a deleted Step or by an Ending that gained a Choice stays in the document until an Ending picks it up and drops it (the seed documents are never pruned behind the Author's back); (e) the "Add choice" form's target stays a native `<select>` — the ticket names the Choice row's target; (f) the marked Choice row is the selected arrow's Choice: one account of "the Choice in hand" for the map and the panel. Derived DoD from the footer, `docs/agents/testing.md`, and `CLAUDE.md`: DoD-1 verified run command green; DoD-2 every PASS artifact committed under `test-results/`, fixture journeys only, no participant data. No package, schema-migration, or lockfile change: `lucide-react` (already a dependency) supplies the ellipsis icon; the document contract is untouched (`validateForPublish`, the runner, analytics, and the seed documents unchanged, as the ticket says). Run surface: local + deployed; per Paul's 2026-09-20 decision there is no post-merge staging smoke gate. No human gates at plan time.

**Availability:** `Blocked by` 21 is `done` on `staging` (`c8c48cb`, PR #25 merged); Paul's order (22 → 23 → 17) names 22 next.

**Repository delivery:** `journeys`, base `staging` @ `c8c48cb`, branch `feat/22-canvas-simplification`, direct checkout, no worktrees. Deliverables run **sequentially**: every one verifies through the one e2e port (3100) and the one `journeys_e2e` database, and the predicted file overlaps are concrete — D2 and D3 both rewrite `src/components/journeys/journey-canvas.tsx` (the `StepNode` toolbar and handles, the `useMemo` that builds nodes and edges, the `CanvasFlow` effects and the `ReactFlow` props), `src/components/journeys/draft-editor.tsx` (`selectStep`, the state block, the canvas props), `src/components/journeys/editor-shared.ts` (`SelectStepOptions`), and `e2e/canvas.spec.ts`; D1 and D3 both rewrite `step-panel.tsx` and `choice-list.tsx` (D3 marks the row D1 rebuilds). Re-checked at closeout against the real diffs.

**Resolved technical decisions (execution, not contract changes):**

- **Shared combobox (D1).** New `src/components/journeys/combobox.tsx`: the pieces `find-step.tsx` already has — the `Input` with `role="combobox"`, `aria-autocomplete="list"`, `aria-expanded`, `aria-controls` (only while open), `aria-activedescendant`; the `ul role="listbox"` of `li role="option"` with `aria-selected` and an `aria-label` that is the option's name alone; ArrowUp/Down wrapping, Enter choosing, Escape closing then clearing, `onMouseDown` prevented on an option so the blur does not close the list under the click; the "No … match" line. Generic over the option: `{ id, name, render? }` plus a filter on `name`, an optional trailing action option (for "New step…" and "Create outcome “<label>”") and `onChoose(id)`. `FindStep` is rebuilt on it with no change to its behaviour or its accessible shape (the existing `canvas-find-step*` tests are the guard). Closed, a field that names a current value shows that value's name and selects it on focus so typing replaces it; blur without a choice restores it.
- **Choice target (D1).** `ChoiceList`'s row target becomes the combobox: `aria-label="Choice target"`, the field showing the target's `stepName` ("Missing step" while dangling), the listbox every Step in map order — `ChoiceList` gains `order: string[]`, handed down from the editor's `stepOrder` through `StepPanel` — the current Step included, filtered by title as the Author types, "New step…" as the trailing option keeping today's `retargetChoiceToNewStep` path; choosing retargets through `updateChoice`. "Open" stays. The e2e assertions that read the target `<select>`'s value by id (`step-editing.spec.ts`) read the field's text or the `draft` row instead.
- **Outcomes from the Ending (D1).** `src/lib/graph/edit.ts` gains two pure edits, tested at Seam A in `edit.test.ts`: `setEndingOutcome(document, stepId, outcomeId | null)` tags the Ending (identity when nothing changes; refused — identity — when the Step is not an Ending or the Outcome does not exist) and then removes the Outcome it dropped when no Ending carries it any longer (`removeOutcome`); `createOutcomeForEnding(document, stepId, label)` adds the Outcome and tags the Ending in one edit, dropping the previous one the same way. `OutcomeList` and its section are deleted. The Ending's field is the combobox with `aria-label="Outcome"`: options every Outcome as "<label>" with its Ending count rendered beside it (`endingCountsByOutcome`), "No outcome" first, and — when the trimmed query matches no Outcome's label exactly — the trailing option "Create outcome “<query>”". A "Rename" button beside the field (present only while the Ending carries an Outcome) swaps the field for an `Input` labelled "Outcome label" holding the label; Enter or blur commits through `renameOutcome`, Escape cancels; the field comes back. The "n steps · m outcomes" summary is untouched.
- **"Leads here from" (D1).** `LeadsHereFrom` and its group are removed from `StepPanel`; `choicesTargeting` stays (the delete confirmation uses it). The e2e walk that went back through the group goes back through "Find step".
- **The view stays put (D2).** `SelectStepOptions` gains `zoom?: boolean` (make the Zoom-to-step move, decision (b)) and `keepView?: boolean` (move nothing); `center` is replaced by `zoom` (`FindStep` passes `zoom: true`). `locate` becomes `{ request, view: "keep" | "reveal" | "zoom" }`: `reveal` is today's rule (bring the box onto the map only when it is off it), `zoom` always makes the move, `keep` does nothing. The editor's `selectStep` asks for `zoom` when the panel was hidden at the moment of the opening (`panelShownRef.current === false`) unless `keepView` was asked for; "Add step", "Add next step", "Duplicate", and the drop on empty map open their new Step with `zoom: true`; `removeStep` opens the Start with `keepView: true`. `applyPanelShown` bumps `fitRequest` only when `remember` is true — "Hide panel", "Show panel", Escape — so an opening that reveals the panel narrows the frame and fits nothing. In `journey-canvas.tsx` the fit-to-all on a change of `nodeIdKey` is deleted, and the locate effect no longer stands aside when the set of boxes changed (a just-added box is the one to zoom to). The zoom move must land on the frame the map ends up with: the canvas notes the moment a `zoom` request arrives exactly as it notes a `fitRequest` (`FIT_AFTER_TOGGLE_MS`), runs the move at once and again on each pane resize inside that window, so a reveal's sliding columns end with the box on the map. If a just-added box is unmeasured when the move runs (`fitView` on it does nothing), run the move again once React Flow reports its dimensions; the e2e proves the new box fully on the map. The fits on first render, on a direction change, and on Hide/Show stay.
- **Compact toolbar (D2).** `CanvasFlow` holds `toolbar: { stepId: string; expanded: boolean } | null`; `onNodeClick` on a Step box sets `{ stepId, expanded: false }` (a click on the box already open re-shows it); an effect clears it when `selectedStepId` is no longer its Step (opened from "Find step", a problem entry, an arrow, a delete, a save refusal); `onPaneClick` collapses it. Node data carries `toolbar: "hidden" | "compact" | "expanded"`. The `NodeToolbar` is visible for `compact` and `expanded`, `Position.Top`, `role="group"` `aria-label="<title> actions"` as now; `compact` renders one `Button` (`variant="outline" size="icon-sm"` or the closest the app's `Button` offers) with `aria-label="Step actions"`, `aria-expanded`, a `lucide-react` `Ellipsis` icon (`aria-hidden`); `expanded` renders that button (now `aria-expanded="true"`) followed by the five moves in today's order. Escape anywhere inside the group collapses it and returns focus to "Step actions"; the group's `onClick` still stops propagation. `CanvasActions` gains `onToggleToolbar(stepId)` and `onCollapseToolbar()`. Nothing is shown for the Start on page load.
- **No Ending colouring (D2).** `OUTCOME_COLORS`, `outcomeColor`, `outcomeColor` in node data, `data-outcome-index`, the `[data-outcome-bar]` span, `outcomeLegend`, the top-right legend `Panel`, and `LEGEND_*` are deleted; `outcomeLabel` ("No outcome") stays as text on an Ending box. `layout.ts` drops `outcomeIndex` and `outcomeIndexOf` (nothing reads them), with their cases in `layout.test.ts`; `outcomeId` stays. The `PEEK_CLASS` comment stops naming the legend.
- **The selected arrow (D3).** Edges: `reconnectable: isSelected ? "target" : false`, `zIndex: isSelected ? 1 : 0`, so only the arrow in hand grows a head to drag and it is drawn over the rest. The e2e drags that re-head an arrow click it first.
- **Drop on empty map (D3).** `ReactFlow`'s `onConnectEnd(event, connectionState)`: when `connectionState.fromNode` is a Step, `connectionState.toNode` is `null`, and the pointer was let go over the pane (`(event.target as Element | null)?.closest(".react-flow__pane")`), call the new prop `onConnectToNewStep(fromNode.id)`; the editor applies `addChoiceToNewStep(documentRef.current, stepId, { label: "" })` and opens the new Step with `{ focusTitle: true, zoom: true }`. A reconnect let go on nothing still leaves the Choice as it was (React Flow routes that through `onReconnectEnd`, not `onConnectEnd`).
- **Clicking an arrow (D3).** `SelectStepOptions.focusChoiceId` becomes `markChoiceId`; `selectStep` with it sets `arrowSelection` to that arrow rather than clearing it; `choiceFocus`, `focusChoiceId`, `focusChoiceRequest`, and `ChoiceList`'s label-field map and focusing effect are deleted. `StepPanel` and `ChoiceList` get `markedChoiceId: string | null` = the selected arrow's Choice when it is on the open Step; that row's `li` carries `aria-current="true"` and `ring-2 ring-ring`. `onEdgeClick` calls `onSelectStep(arrow.stepId, { markChoiceId })`; `connectSteps` opens the Step with `markChoiceId: created.choiceId`. No `.focus()` anywhere on either path.
- **e2e (D1–D3).** New helpers in `e2e/canvas.spec.ts`: `stepActions(page, title)` (click the box, expect the "Step actions" button on it), `expandStepActions(page, title)`, `zoomOf(page)` (the zoom read off the settled viewport transform), `dropChoiceOnEmptyMap(page, fromTitle)`; `addOutcome` becomes `tagEndingWithOutcome(page, endingTitle, label)` (open the Ending, type the label, choose "Create outcome …" or the existing Outcome). Every test that used the box toolbar on the open Step, `data-outcome-index`, the legend, the Outcomes section, "Leads here from", or a focused label field after a drag or an arrow click is rewritten to the new behaviour, never skipped; `canvas-legend-outcomes` becomes `canvas-endings-uncoloured`. Recording stays on for the "authoring from the map" describe; AC-10's test joins it.
- **README and `CONTEXT.md` (D3).** README's editor paragraph: the view moves only when asked (first render, a direction change, Hide/Show, and the zoom to an opened or added Step), the "Step actions" button on a clicked box, no legend or Ending colour, the searchable target and Outcome fields, Outcomes made and renamed from an Ending, a Choice dropped on empty map making a Step, an arrow click marking the Choice's row. `CONTEXT.md` Canvas: the same in its register; Outcome: made, chosen, and renamed from an Ending, gone when the last Ending drops it.

**Deliverables (all `atlas-worker`, direct checkout on `feat/22-canvas-simplification`, in this order):**
- **D1 — the panel** (opus): `combobox.tsx` (new), `find-step.tsx`, `choice-list.tsx`, `step-panel.tsx`, `draft-editor.tsx` (drop `OutcomeList`, hand `stepOrder` down), `outcome-list.tsx` (deleted), `edit.ts` + `edit.test.ts`, `e2e/step-editing.spec.ts` (AC-4, AC-6, AC-9), `e2e/canvas.spec.ts` (the Outcome helper and its callers only).
- **D2 — the view and the box** (opus): `journey-canvas.tsx`, `draft-editor.tsx`, `editor-shared.ts`, `find-step.tsx` (`zoom`), `layout.ts` + `layout.test.ts`, `e2e/canvas.spec.ts` (AC-1, AC-2, AC-3 and every toolbar caller).
- **D3 — the arrows and Seam B** (opus): `journey-canvas.tsx`, `draft-editor.tsx`, `editor-shared.ts`, `step-panel.tsx`, `choice-list.tsx`, `e2e/canvas.spec.ts` (AC-5, AC-7, AC-8, AC-10 and every drag or arrow-click caller), README, `CONTEXT.md`.

**Verification map (evidence committed under `test-results/`; the root is cleared once, immediately before the aggregate capture):**

| Criterion | Command / action | Surface & real deps | Expected | Evidence | Earliest | Invalidated by |
|---|---|---|---|---|---|---|
| AC-1 | `pnpm test:e2e` test `canvas-view-stays-put` (case-3 seeded, 1600×1200) | local; docker Postgres `journeys_e2e`; production server on 3100 | with the panel hidden, "Step actions" → "Zoom to step" on a box and the zoom read off the settled transform; clicking another box shows the panel, the settled zoom is ≥ the reading, and the clicked box is fully on the map; "Add next step" from that box's "Step actions" leaves the zoom ≥ the reading and the new box fully on the map; deleting that Step from the panel leaves the settled transform identical | `test-results/canvas-view-stays-put/….png`, `dod-1-e2e.txt` | after D2 | changes under `src/`, `e2e/` |
| AC-2 | test `canvas-step-actions` (fresh Journey) | as AC-1 | on load no "Step actions" button and no "… actions" group on the map; clicking a box shows "Step actions" on that box (`aria-expanded="false"`); clicking it shows Add next step, Duplicate, Zoom to step, Make this the start, Delete step; Escape collapses it (`aria-expanded="false"`, the moves gone, the button focused); opening another Step from "Find step" shows no toolbar | `test-results/canvas-step-actions/….png`, `dod-1-e2e.txt` | after D2 | as AC-1 |
| AC-3 | test `canvas-endings-uncoloured` (case-3 with its three Outcomes); `pnpm test src/lib/graph/layout.test.ts` | as AC-1; Vitest | zero `[data-outcome-bar]`, zero `[data-outcome-index]`, no list named "Legend" on the map; the layout no longer carries an Outcome index | `test-results/canvas-endings-uncoloured/….png`, `ac-3-layout.txt`, `dod-1-e2e.txt` | after D2 | as AC-1 |
| AC-4 | test `panel-choice-target-search` in `step-editing.spec.ts` | as AC-1 | typing part of another Step's title in a Choice row's target field lists that Step; choosing it retargets the Choice; the `draft` row read back holds the new target; the arrow's accessible name names the new target | `test-results/panel-choice-target-search/….png`, `dod-1-e2e.txt` | after D1 | as AC-1 |
| AC-5 | test `canvas-retarget-selected-arrow` | as AC-1 | two Choices from two Steps into one box; the second arrow clicked, its head dragged onto a third box; the `draft` row shows the second retargeted and the first unchanged | `test-results/canvas-retarget-selected-arrow/….png` + `.webm`, `dod-1-e2e.txt` | after D3 | as AC-1 |
| AC-6 | assertion in `panel-choice-target-search` | as AC-1 | no group named "Leads here from" in the panel on the Start, on a Step with incoming Choices, and on one without | `test-results/panel-choice-target-search/….png`, `dod-1-e2e.txt` | after D1 | as AC-1 |
| AC-7 | test `canvas-drop-choice-on-empty-map` | as AC-1 | dragging from a box's connect dot onto bare map creates a Step and a Choice to it; the panel opens on the new Step with its title focused; the new box is fully on the map; the `draft` row holds both | `test-results/canvas-drop-choice-on-empty-map/….png` + `.webm`, `dod-1-e2e.txt` | after D3 | as AC-1 |
| AC-8 | test `canvas-arrow-click-stays-on-canvas` (a viewport short enough that the page scrolls, scrolled to the map) | as AC-1 | clicking an arrow opens its Step with that Choice's row `aria-current="true"`; `document.activeElement` is inside the Canvas region; `window.scrollY` is what it was before the click | `test-results/canvas-arrow-click-stays-on-canvas/….png`, `dod-1-e2e.txt` | after D3 | as AC-1 |
| AC-9 | test `panel-outcomes-from-the-ending` in `step-editing.spec.ts`; `pnpm test src/lib/graph/edit.test.ts` | as AC-1; Vitest | no "Outcomes" section beneath the map; on an Ending, typing "Reached care" and choosing "Create outcome “Reached care”" tags it and the summary reads "1 outcome"; a second Ending picks it from the list; "Rename" renames it for both (the `draft` row read back); "No outcome" on both removes it and the summary reads "0 outcomes"; Seam A: `setEndingOutcome` and `createOutcomeForEnding` tag, drop, and prune as specified | `test-results/panel-outcomes-from-the-ending/….png`, `ac-9-outcome-edits.txt`, `dod-1-e2e.txt` | after D1 | as AC-1 |
| AC-10 | test `canvas-build-by-dropping-and-walk` (recorded) | as AC-1 | a three-Step branch built by dropping two Choices on empty map, both Endings tagged with one Outcome made from the panel, validated, published, and walked by a Participant to an Ending; screenshots of the map and the runner and the recording | `test-results/canvas-build-by-dropping-and-walk/….png`, `…-runner.png`, `….webm`, `dod-1-e2e.txt` | after D3 | as AC-1 |
| DoD-1 | `DATABASE_URL=postgresql://postgres:postgres@localhost:5436/journeys pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build && DATABASE_URL=postgresql://postgres:postgres@localhost:5436/journeys pnpm test:e2e` | local; docker Postgres | all exit 0; no test retried; no console noise in the e2e log; every existing canvas, draft, step-editing, and publish spec passes | `test-results/dod-1-commands.txt`, `test-results/dod-1-e2e.txt` | after D3 | any source, config, or test change |
| DoD-2 | evidence review at closeout | local | every PASS artifact committed; fixture journeys only | this ticket's `[CLOSEOUT]` | closeout | — |

### [PROGRESS] 2026-09-21 — D1–D3 integrated; aggregate review started

- **D1** — `atlas-worker` on opus — `2645751`: `combobox.tsx`, the editor's one combobox (Find step's ARIA shape: `role="combobox"`, `aria-autocomplete="list"`, `aria-expanded`, `aria-controls` while open, `aria-activedescendant`; `role="listbox"` of `role="option"` named by the option alone; wrapping arrows, Enter, Escape list-then-query; a value mode that shows the chosen name, selects it on focus, and restores it on blur; a trailing `action` option handed the typed query). `FindStep` rebuilt on it unchanged in behaviour. The Choice row's target is the combobox over the Steps in map order (`order` handed down from the editor's `stepOrder`), "Missing step" while dangling, "New step…" trailing. The Ending's Outcome field is the combobox over every Outcome with its Ending count, "No outcome" first, "Create outcome “<label>”" trailing when no Outcome has that label, and a "Rename" button that swaps in an "Outcome label" input (Enter/blur commits through `renameOutcome`, Escape cancels). `setEndingOutcome` and `createOutcomeForEnding` in `edit.ts` (identity on refusal; the dropped Outcome pruned through `removeOutcome` when no Ending carries it) with fifteen Seam A cases. `LeadsHereFrom` and `OutcomeList` deleted. e2e `panel-choice-target-search` (AC-4, AC-6) and `panel-outcomes-from-the-ending` (AC-9) in `step-editing.spec.ts`; `addOutcome` became `tagEndingWithOutcome` in `canvas.spec.ts`. Worker-run: 60 edit cases, 237 unit, 31 e2e green.
- **D2** — `atlas-worker` on opus — `67a150b`: `SelectStepOptions.zoom`/`keepView` replace `center`; `locate.view` is `keep | reveal | zoom`; `selectStep` asks for `zoom` when the panel was hidden at the opening; "Add step", "Add next step", "Duplicate" open with `zoom: true`; a delete opens the Start with `keepView: true`; only the Author's Hide/Show/Escape bump `fitRequest` (`setPanelByAuthor`). The fit on a node-set change is gone; `zoomToStep` is `fitView` on the box with `maxZoom: Math.max(1, zoom)`, retried a frame at a time (≤30) until React Flow has measured a just-added box; one `viewRequest` ref (`at`, `box | null`) drives the 500 ms resize window for both whole-map fits and box zooms. The toolbar is `{ stepId, opening, expanded }` set by a box click and shown only while `opening === locate.request` and the Step is open: one "Step actions" button (`Ellipsis`, `aria-expanded`) expanding to the five moves; Escape collapses and refocuses it; a pane click collapses it. `OUTCOME_COLORS`, the colour bar, `data-outcome-index`, the legend, and `layout.ts`'s `outcomeIndex` deleted. e2e `canvas-view-stays-put` (AC-1), `canvas-step-actions` (AC-2), `canvas-endings-uncoloured` (AC-3, replacing `canvas-legend-outcomes`); every toolbar and fit-on-add caller rewritten; `fitWholeMap` now settles the map and restores the page scroll (two real causes, both left-to-right failures). Worker-run: 22 layout cases, 237 unit, 64 e2e green.
- **D3** — `atlas-worker` on opus — `db38f0a`: only the selected arrow is `reconnectable` and it carries `zIndex: 1`; `onConnectEnd` makes a Step and a Choice for a drag that began on the connect dot, ended on no box, and was let go over the pane (`connectToNewStep`, opened with the title focused and zoomed to); `focusChoiceId` → `markChoiceId`, which sets the arrow selection rather than clearing it, so the marked row (`aria-current="true"`, `ring-2 ring-ring`) is the selected arrow's Choice; no focusing of label fields anywhere; `onEdgeClick` hands the keyboard to the Canvas section with `preventScroll` (Chromium leaves a clicked edge unfocused, and AC-8 wants the active element inside the Canvas); a 250 ms `CLICK_AFTER_CONNECT_MS` window keeps the browser's post-drag click on a self-loop from re-opening the Step and dropping the mark. e2e `canvas-retarget-selected-arrow` (AC-5), `canvas-drop-choice-on-empty-map` (AC-7), `canvas-arrow-click-stays-on-canvas` (AC-8, 1280×640), `canvas-build-by-dropping-and-walk` (AC-10, recorded); every head-drag caller clicks the arrow first and every label-focus assertion became the marked row. README's editor paragraph and `CONTEXT.md` (Canvas, Outcome) updated. Worker-run: 237 unit, whole e2e suite 68 green.
- Orchestrator candidate run at `db38f0a` (local, docker Postgres 5436, production server on 3100): lint, format:check, typecheck, unit (237), build, and the whole e2e suite (68 passed, `retries` 0) all exit 0. Candidate only; the aggregate capture follows the review. Status `in-progress` → `ai-review`.

### [AI CODE REVIEW] 2026-09-21 — aggregate review of `c8c48cb..db38f0a`, fixes in D4 (`a9e516d`)

Two fresh reviewers (one per axis, opus) read the whole diff against the ticket, the execution plan, `CONTEXT.md`, both ADRs, `docs/agents/testing.md`, `CLAUDE.md`, the ESLint/Prettier config, the `staging` versions of the rewritten files, and the installed `@xyflow/react` / `@xyflow/system` source (`onConnectEnd` at the end of a reconnect drag, `FinalConnectionState`, edge `zIndex` layers, `getInternalNode().measured`). The orchestrator adjudicated every candidate from the cited hunks. Both reviewers confirmed: all ten "What to build" bullets built; every AC has a named test that fails without the feature, none skipped, no `.only`, no fixed waits, no widened timeouts; expected values independent (the `draft` row read back from Postgres, ids resolved by title); declared scope respected (`validate.ts`, the runner, analytics, `scripts/seed/`, and the document schema untouched); every withdrawn behaviour gone with no dangling reference to `focusChoiceId`, `outcomeIndex`, `OUTCOME_COLORS`, `OutcomeList`, `LeadsHereFrom`, `data-outcome-bar`, `data-outcome-index`, or `locate.center`; readings (a)–(f) honoured; `src/lib/graph/*` pure; no `_Avoid_` term anywhere new; `retries` 0. **No blocking finding on either axis.**

**Deviations approved during acceptance (before the review):** D2 derives the toolbar's visibility from `locate.request` (`toolbar.opening === locate.request && stepId === selectedStepId`) rather than clearing it in an effect (the repo's `react-hooks/set-state-in-effect` rule), and `setPanelByAuthor` bumps `fitRequest` for Hide/Show/Escape while a reveal bumps nothing; D2's e2e `fitWholeMap` settles the map and restores `window.scrollY` after the Controls' fit-view (two real causes of the left-to-right failures); D3 guards `onConnectEnd` on `fromHandle.id === "connect"` because React Flow 12.11.6 also calls it at the end of a reconnect drag (the plan's premise was wrong; verified in `@xyflow/system` 0.0.82); D3 gives the keyboard to the Canvas section on an arrow click (`preventScroll`) because Chromium leaves a clicked edge unfocused and AC-8 asks for the active element inside the Canvas; D3 ignores the click the browser fires within 250 ms of a Choice being let go of (`CLICK_AFTER_CONNECT_MS`), which otherwise re-opened the Step and dropped the mark after a self-loop drag; the bare-map leg of `canvas-connect-and-retarget-by-dragging*` stays a reconnect head dropped on nothing (Choice unchanged), AC-7's positive case having its own test and Seam B.

**Axis 1 — technical implementation and spec conformity** (7 candidates)

| # | Severity | Paths | Disposition |
|---|---|---|---|
| T1 the combobox list is laid out inside the panel column's `overflow-hidden` box (load-bearing for the slide), so a field low in a tall panel has its 320 px list clipped with no way to scroll to the rest; Playwright's `toBeVisible` does not see ancestor clipping | non-blocking | `combobox.tsx`, `draft-editor.tsx` | resolved in D4: on open a `useLayoutEffect` measures the room below the field to the nearest ancestor whose `overflow-y` is not `visible` (and the window) and opens the list above the field when that room is under 320 px and the room above is larger; `panel-choice-target-search` builds the Start out to eight Choices and asserts the drawn list inside the panel column and the viewport with "New step…" reachable (red first: the list ran 127 px past the column) |
| T2 `zoomToStep` re-read the live zoom as its ceiling on every resize replay inside the 500 ms window, so a reveal whose first fit lowered the zoom fed that lower zoom back as the next ceiling (a ratchet) | non-blocking | `journey-canvas.tsx` | resolved in D4: the ceiling is read once when the request is made (`viewRequest.maxZoom`) and every replay is held to it |
| T3 `viewRequest` was never cleared, so a `keepView` opening (a delete) that resized the pane inside the window replayed the previous move | non-blocking | `journey-canvas.tsx` | resolved in D4: the `keep` branch and a lapsed window both null the request |
| T4 the rename committed an untrimmed, possibly empty label; "Create outcome" compared labels exactly, so `"reached care"` and `"Reached care "` could be created beside `"Reached care"` | non-blocking | `step-panel.tsx` | resolved in D4: trimmed, an empty label cancels the rename, the offer compares trimmed and case-insensitively; both asserted in `panel-outcomes-from-the-ending` |
| T5 toolbar visibility keyed to the predicted `locate.request + 1` couples the canvas to how `selectStep` counts openings | non-blocking | `journey-canvas.tsx` | deviation: the approved D2 derivation; the coupling is documented at the state's declaration; kept |
| T6 `onEdgeClick` takes focus unconditionally, so an Author walking the map by keyboard loses their box on an arrow click | non-blocking | `journey-canvas.tsx` | deviation: D4 tried the conditional guard and it regressed `canvas-arrow-click-stays-on-canvas` — measured sequence `mousedown` on the box, `focusin` on the edge's `g` (React Flow's arrows are focusable), then focus dropped to `body` as the arrow is redrawn selected, so "already inside the Canvas" at click time says nothing about a frame later; the unconditional focus is kept and the measurement recorded in the comment |
| T7 "the selected arrow is drawn above the others" had no assertion | non-blocking | `canvas.spec.ts` | resolved in D4: React Flow renders each edge in its own `<svg style="z-index">`; `canvas-retarget-selected-arrow` asserts the selected arrow's layer has the greater computed `z-index` |

**Axis 2 — coding standards** (10 candidates, all non-blocking)

| # | Paths | Disposition |
|---|---|---|
| S1 the deleted test's committed artifact `test-results/canvas-legend-outcomes/` | `test-results/` | resolved by the proof-root clear before the aggregate capture |
| S2 the Outcome-tagging helper and `arrowLabelled` duplicated across the two specs | `canvas.spec.ts`, `step-editing.spec.ts` | resolved in D4: `tagWithOutcome` and `arrowLabelled` live in `e2e/setup/authoring.ts` |
| S3 `Combobox`'s memo defeated by unstable `options`/`action` in two of three consumers | `step-panel.tsx`, `choice-list.tsx` | resolved in D4: `useMemo`/`useCallback` in `OutcomeField`, a module-level `newStepOption` in the row |
| S4 `nodeViews`' comment still named the legend | `canvas.spec.ts` | resolved in D4 |
| S5 README editor paragraph left ragged where sentences were cut | `README.md` | resolved in D4: re-flowed |
| S6 "coloured"/"uncoloured" against the repository's American spelling | `README.md`, `canvas.spec.ts` | resolved in D4: "colored"; the test and its evidence path renamed `canvas-endings-uncolored` |
| S7 `await (await expandStepActions(…))` at five sites | `canvas.spec.ts` | resolved in D4 |
| S8 an unnamed 400 in `canvas-view-stays-put`'s neighbour count | `canvas.spec.ts` | resolved in D4: `NEIGHBOUR_REACH` |
| S9 `CONTEXT.md`'s Canvas entry carried less than the plan said | `CONTEXT.md` | resolved in D4: the drop on bare map, the one arrow with a head, the marked row |
| S10 "Step actions" said `aria-expanded` with no `aria-controls` | `journey-canvas.tsx` | resolved in D4: the moves wrapped in an identified `div`, named while expanded |

**Remaining risks:** (1) the 250 ms post-drag click window is a timing heuristic about a browser-generated click — invisible to an Author, but a constant; (2) the toolbar's `opening === locate.request + 1` prediction holds only while `selectStep` is the sole writer of `locate` and bumps it by one; (3) a Controls "fit view" click inside the 500 ms window after a reveal is answered by the reveal's own zoom; (4) an Outcome orphaned by a deleted Ending or by an Ending that gained a Choice stays in the document and in the "m outcomes" count until an Ending picks it up and drops it (reading (d)); (5) the list's above/below placement is measured once as it opens, not again if the panel grows while it is open; (6) an arrow click always moves the keyboard to the Canvas section, so a following Escape hides the panel (ticket 21's rule) — consistent, but a behaviour the ticket did not name; (7) the Zoom-to-step ceiling is read at the moment of the request, so a click landing mid-animation of a Hide fit takes the intermediate zoom as "before"; (8) `panel-choice-target-search` builds eight Steps to reach the list's cap and is ~8 s longer; (9) the `Combobox` itself is not `React.memo`'d, so what S3 stabilises is its internal memo, not its render.
