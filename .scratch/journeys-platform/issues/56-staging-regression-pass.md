# 56: The pre-hackathon regression pass on staging

Status: done
Blocked by: 54, 55
Owner: Claude (Fable 5.1), 2026-09-24
Parent: `.scratch/journeys-platform/spec.md`
Priority: hackathon presentation (Paul, 2026-09-24, grilled the same evening): runs after 54 and 55 merge to `staging` and before Paul promotes `staging` → `main` for the 10:00 ET 2026-09-25 deadline.
Route: polish (no code; findings become tickets)

**Why:** Paul, 2026-09-24: "what I am looking for here is a more automated usage of the app and checks for obvious quality gaps." The e2e suite proves behaviour; it never looks at copy, layout at odd widths, dark-mode oddities, empty states, or the feel of a first visit, which is what a judge sees.

**What is true today:** every ticket runs its own specs and CI runs the whole suite, but nobody has walked the deployed app end to end as a stranger since round 4. Staging will be at `https://journeys-staging.paul-macfarlane.com` with the seed Project published (54's Comments).

**Decisions (Paul, 2026-09-24):** an agent thread walks staging in the desktop app's browser pane against a checklist and writes findings. Paul signs in inside the pane himself (OAuth; the agent never enters credentials). A Playwright tour spec against the deployed URL is the post-hackathon version (see Comments).

**What to do:**

1. Anonymous, both themes, desktop and 375 px: `/`, `/about`, `/guide`, `/privacy`, `/terms`, the seed Project page, play one case to an Ending, go back where allowed, paste the root URL into a chat to check the link preview, `/authors/<Paul's id>`.
2. Signed in (Paul signs in once): create a Project and a Journey; on the canvas add three Steps and Choices by dragging, switch direction, fit, undo, redo; write rich text with an image and a credit; add a Prompt with an AI-decided Choice; pick a Theme; publish; Preview; play the Published Version anonymously in a private window including the Prompt; open analytics and confirm the Run is drawn; restore the earlier Version; invite a Member; open the Author page; delete the test Journey and Project.
3. Every page: keyboard-only pass of the main flow, console errors, an accessibility scan with axe (the browser pane can run `axe-core` from a CDN in `javascript_tool`), text overflow at 375 px, dark-mode contrast.

**Deliverables:** a `[FINDINGS]` record in this ticket's Comments listing each finding as `severity — page — what happened — what was expected`, with a screenshot path under `test-results/56-regression-pass/`. Severity `blocks-promotion` for anything a judge would hit in the first two minutes or that loses data; `fix-tonight` for visible copy and layout faults; `post-hackathon` for the rest. Each `blocks-promotion` and `fix-tonight` finding becomes its own ticket (numbered from 58) with `Route: polish`; the rest go on one `post-hackathon` ticket. The thread fixes nothing itself. Paul decides which findings hold the promotion.

Acceptance criteria:

- [x] Every step in the three lists above was walked on staging, in both themes, and the `[FINDINGS]` record says so, including "no finding" sections.
- [x] Every finding has a severity, a screenshot, and (for the top two severities) a ticket.
- [x] The test Project and Journey created in step 2 are deleted afterwards, and no screenshot contains a real participant Response.

Verification and evidence follow `docs/agents/testing.md`: no command chain (no code changes); evidence is the screenshot directory named above. Use `CONTEXT.md` vocabulary. Spec: `.scratch/journeys-platform/spec.md`. Origin: Paul, 2026-09-24, at 54's grilling.

## Comments

### 2026-09-24 — post-hackathon follow-up (not this ticket)

A `tour` Playwright spec run against a deployed URL (`E2E_BASE_URL`), minting a session with the existing helpers, that performs list 2 above and an axe scan of every public route, reporting rather than asserting. File it when 56 closes.

### 2026-09-24 — Claude (Fable 5.1), `[FINDINGS]`

**Where and how.** Staging was `https://staging-journeys-ten-virid.vercel.app` (deployment `df348ae`, the `staging` head with 54 and 55 merged); the custom staging domain has no DNS record yet. Staging sits behind Vercel Deployment Protection, so the anonymous half ran through a 23-hour share link (Vercel MCP `get_access_to_vercel_url`, cookie `_vercel_jwt`) in headless Chromium: every public route in four combinations (1280 px and 375 px, light and dark), each with a full-page screenshot, axe-core, console and failed-request capture, a horizontal-overflow and clipped-text probe, and a Tab-order pass. The seed Project did not exist on staging until Paul seeded it mid-pass and its cases are still unpublished there, so "play one case to an Ending" ran against production's seed (`journeys-ten-virid.vercel.app`, `main` at `c0fc678`), whose runner and public pages differ from staging only in the footer (`git diff origin/main origin/staging -- src/components/runner src/app/j src/app/p`). The signed-in half ran in the desktop app's browser pane at its natural 646 px width with Paul signed in (pointer clicks under viewport emulation never reached the page, so no emulated-width clicks); its screenshots cannot be saved from the pane, so the one signed-in finding that needs a picture was reproduced locally on the same commit with a minted session. Evidence: `test-results/56-regression-pass/` (80 files; `<width>-<theme>-<route>.png` for staging routes, `prod-…-walk-…` and `prod-…-keyboard-…` for the production walk, `…-play-…` for the anonymous play of the test Journey on staging, `local-…` for the local reproduction, `preview-…-production.png` for the crawler-style link-preview fetch).

**Findings.** `severity — page — what happened — what was expected — screenshot — ticket`.

- **blocks-promotion** — `/p/00000000-5eed-4000-8000-000000000001` on staging — before the seed, a bare Next 404 from the splash's Play link; after the seed, "No journeys are available right now" because no case is published on staging. Production has the three cases published on the old build. Expected: the Play link lists Case 1–3 wherever the judges land. Also: `journeys.paul-macfarlane.com` resolves but is not attached to the Vercel project, and the staging domain has no DNS; `metadataBase` follows `BETTER_AUTH_URL`. — `1280-light-keyboard-project.png`, `1280-light-seed-project.png` — **59** (Paul's promotion gate).
- **fix-tonight** — every unknown route — Next's bare "404 | This page could not be found." with no app shell, no link home, no `<main>` landmark (axe `landmark-one-main`, `region`), and an inconsistent title. Expected: a Journeys page with the header, a sentence, a home link, and the Play link. — `1280-light-nope.png`, `375-dark-nope.png`, `1280-dark-allotment-project.png` — **60**.
- **fix-tonight** — the seed cases (production and staging) — 12 of the 36 stored image URLs return 400/403/404/410 from a desktop Chrome user agent (NYT, rawpixel, picryl, one Wikimedia thumbnail, one WordPress); Case 1's most-walked Ending "Wait w/ Broken Leg" shows its credit line with no photo. Expected: every image loads or the image is removed. — `prod-1280-light-walk-step-20.png` — **61**.
- **fix-tonight** — `/about`, `/guide`, `/privacy`, `/terms` at 1280 px — the footer wraps to two rows (Privacy and Terms under the mark) because `ProsePage` narrows it to the prose column and 54 added two links; the splash, sign-in, runner, and Author page keep one row. — `1280-light-privacy.png`, `1280-dark-about.png` vs `1280-light-sign-in.png` — **62**.
- **fix-tonight** — the runner's Choices, Next, Continue, Start over — keyboard focus shows only a faint 50 % ring on the outline button; imperceptible in dark. Expected: a visible focus outline in every Theme. — `prod-1280-light-keyboard-focus-choice.png`, `prod-375-dark-keyboard-focus-choice.png` — **63**.
- **fix-tonight** — the Journey page header, on Publish — nothing acknowledges a successful publish beyond a disabled button, a badge change, and two new controls; the new Version's number and the link are not offered (Paul, 2026-09-24, on this PR: "Right now its not clear"). Expected: one visible "Published Version N — participants see it now" line with the link. — no screenshot (the pane cannot save one; the `publish` spec will) — **65**.
- **post-hackathon** — fifteen smaller items (page titles on the Project and Journey pages, editor axe: attribution contrast and heading structure, one-Step fit view at maximum zoom, image bubble menu overlap, "Copy participant link" name on the Author-page copy button, UTC timestamps on Versions, accent default ignoring the preset, missing placeholders, headerless public pages, silent deep-link fallback, unlinked sign-in copy, staging `og:image` on the protected alias, hero video pausing in the desktop pane, "sign up" in the Members refusal, the judge's "100%" in Preview) — `local-375-dark-new-journey.png`, `local-1280-light-new-journey.png` — **64**.

**No finding (walked, both themes, desktop and 375 px unless noted).**

- List 1: `/`, `/about`, `/guide`, `/privacy`, `/terms`, `/sign-in`, the seed Project page, the test Project page, the Author page: 200, zero axe violations (contrast included) in all four combinations, no horizontal scroll and no clipped text anywhere (the 375 px Guide is 14 632 px tall and clean), console clean apart from the 404 resource line on 404 pages, only cancelled `_rsc` prefetches in the failed-request log. Case 1 played 20 Steps to the Ending "Wait w/ Broken Leg" in all four combinations with zero axe violations on the first and last Step; browser Back returned to the previous Step and Forward restored it; Start over returned to the Preface. Link preview: `og:title`, `og:description`, `og:image` (1200 × 630 PNG, `image/png`, 53 KB), `twitter:card summary_large_image` on `/`, and a Project-specific title and 1200 × 630 image on `/p/<seed>` on production (`preview-home-production.png`, `preview-seed-project-production.png`); staging's tags are the same shape but its image host is the protected alias (finding above). Keyboard: on `/` the first Tab lands on the Play link; on the Project page the first Tab is Case 1; on a Step the first Tab is Next or the first Choice; Enter advances at every stage.
- List 2 (signed in, 646 px pane; 1280 px and 375 px where a screenshot exists): New project and New journey dialogs create and land on the new page; three Steps made by dragging the connect dot onto empty map, each opening in the panel with its title focused and the map title updating live; Left to right and Top to bottom, Fit View, Undo twice (direction and a rename) and Redo twice all correct; Choice labels, Add choice with a target, Prompt with "Required" and "AI decides" (disabled with "Needs two or more choices." until the second Choice); rich text with H1, paragraph, image with alt text and a Caption carrying the credit line, from an absolute URL, quote, and bullet list, saved and rendered in the runner; Theme "Use a different theme" → Dusk plus a plum accent, saved; Publish without a confirm, "Published" badge and Copy link; Preview walks the Draft with the Theme and, on the deciding Prompt, shows "The judge picked "Go home" (100%) — a live run would have advanced." with the Choices; anonymous play of the Published Version in a fresh headless context at 1280 light and 375 dark: the judge picked "Keep digging" in 1.3 s and 0.8 s, the Ending rendered the image, caption, quote, and list with zero axe violations (`1280-light-play-6-ending.png`, `375-dark-play-2-prompt.png`); Analytics: Starts 2, Completions 2, Begin/Keep digging/Finish 100 % · 2 times, Go home 0 %, both Runs on the graph and in "Runs by outcome"; a second Publish after renaming Start made Version 2 (live), Restore of Version 1 asked "Replace the draft with version 1?" and put the Start title back with "Unpublished changes" and a Draft row; Members: "Add member" with an unknown address refused with "No account has that email — they need to sign up first" (no second account exists to add); Settings: "Public Author page" on with a bio produced `/authors/c4gx4AAffx7Aa8IJIk94cR677zYrtmmI` listing the Projects with a live Journey (`1280-light-author.png`, `375-dark-author.png`), then turned off and the bio cleared; Delete journey and Delete project each confirmed with "Delete permanently" and the Projects list no longer shows the test Project.
- List 3: keyboard order on the Journey page is Back to project → Title → Description → Copy link → Unpublish → Preview → Publish → Delete journey → Editor tab, every stop with a visible ring; axe on the signed-in Journey page (dark) reports only the attribution contrast and heading-structure items in 64; no console errors on any signed-in page (the "preloaded but not used" warnings for `/demo/*.png` belong to the splash visit earlier in the same tab, not to the authoring pages).

**Acceptance, with the deviations stated.** Every step in the three lists was walked, with these departures from the ticket's letter:

- "Play one case to an Ending, go back where allowed" ran against **production's** seed, not staging's, because staging's cases are unpublished (finding A). The runner and public pages are byte-identical between `main` and `staging` apart from the footer, so the result stands for staging's runner; rerun `ONLY=walk WALK_BASE=<staging> …` once Paul publishes the three cases there if a staging-only proof is wanted. That walk wrote **six Runs onto production's Case 1** (four walks to "Wait w/ Broken Leg" and two keyboard passes that stopped after the first Choice); they will show in production analytics for Version 1 of Case 1 and cannot be deleted from the app.
- The walk ran in all four combinations and the JSON report holds all four, but only the 1280 light and 375 dark screenshots are committed; the other two were identical and were dropped to keep the evidence at 20 MB.
- The anonymous half ran in headless Chromium rather than the browser pane, because the deliverable needs screenshot files, axe, and console capture, none of which the pane can write to disk; the pane was used for the first look at the splash, the sign-in page, and the whole signed-in half.
- The signed-in half ran at the pane's 646 px width only (its viewport emulation drops pointer clicks); 1280 px and 375 px coverage of authoring surfaces is limited to the local reproduction of a new Journey and the anonymous play of the test Journey. Axe and the keyboard pass on signed-in pages were run on the Journey page; the Project page, Members, Versions, Analytics, and Settings were read but not scanned.
- "Invite a Member" exercised the refusal path only ("No account has that email"); there is no second account on staging to add.
- Tickets are numbered from **59**, not 58, because 58 already exists (the demo Journey).

The test Project and Journey are deleted; Paul's Author page is off and his bio empty again; no screenshot contains a real participant Response (the two Runs on the test Journey were the pass's own, on a Journey that no longer exists; the production Runs above carry no Response). Each `blocks-promotion` and `fix-tonight` finding has a ticket (59–63); the rest are on 64.

### 2026-09-24 — Claude (Fable 5.1), `[CLOSEOUT]`

**PR:** https://github.com/paul-macfarlane/journeys/pull/70 (base `staging`, head `chore/56-regression-pass`, commits `c80fbf4` anonymous evidence, `361a36c` findings and tickets, plus this closeout). Route: polish; delivered with `/implement` in the main checkout, no worker delegation, no code.

**Delivered:** the `[FINDINGS]` record above; tickets 59 (`ready-for-human`, Paul's promotion gate), 60–63 and 65 (`ready-for-agent`, `Route: polish`; 65 is Paul's own publish-acknowledgement finding, added on the PR), and 64 (`needs-triage`, the post-hackathon holding ticket); `test-results/56-regression-pass/` (80 PNG files, 20 MB). The scripts that produced the anonymous evidence (`tour.ts`, `play.ts`, a scratch Playwright spec for the local reproduction) lived under `.claude/atlas-state/56/` and `e2e/` and are not committed; the post-hackathon `tour` spec in the first Comment is the durable version.

**Verification:** no command chain (no code changes). Evidence is the screenshot directory the ticket names; the `dod-1-commands.txt` capture does not apply. CI on the PR runs the unchanged suite.

**AI review** (`/code-review` against `staging`, one pass, two axes): Standards flagged "credit" and "node" against `CONTEXT.md`'s Caption and Step entries (fixed: Caption and image; "credit line" kept for the seed's own photo-credit paragraphs) and noted that the evidence lives in a per-ticket directory rather than per-test directories (by the ticket's own instruction; noted here for the maintainer). Spec flagged that the case walk ran on production, that only two of four walk combinations kept screenshots, that the Member invite was the refusal path only, the 646 px limit on the signed-in half, the headless browser for the anonymous half, the six Runs written to production's Case 1, and the numbering from 59; all are now stated in the "Acceptance, with the deviations stated" paragraph. Nothing else applied.

**For Paul:** merge #70, then work 59 (your gate) and decide which of 60–63 hold the promotion; 61 is the one a judge is likeliest to notice. Publish the three cases on staging if you want the walk re-run there (`ONLY=walk WALK_BASE=https://staging-journeys-ten-virid.vercel.app` with the same share link, valid until 2026-09-25 17:05 ET). Production's Case 1 analytics now carry six Runs from this pass.
