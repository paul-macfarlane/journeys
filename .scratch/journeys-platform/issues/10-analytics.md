# 10: Analytics

Status: done
Blocked by: 06, 09
Owner: Claude Fable 5.1 (/implement, 2026-09-22)
Parent: `.scratch/journeys-platform/spec.md`
Route: contract

**What to build:** A Member opens a published Journey's analytics for a chosen Published Version and sees the canvas with numbers on it — percentage taking each Choice, Run count on each Ending, abandonment count on each Step — plus a Runs-by-Outcome chart and starts/completions totals. All computed from Run paths (no event table). Members-only. The Runs-by-Outcome chart groups untagged Endings each by its own Step title, so "40% reached 'Turned back'" still reads (from ticket 24, 2026-09-22). A Journey whose Start Step is an Ending records no Run at all — a Run is created by the Participant's first Choice, and such a Journey offers none — so its starts, completions, and Ending counts read zero however many people open it (from ticket 27, 2026-09-22).

- [x] Seam A: aggregation functions produce hand-computed expectations for choice take-rate, ending counts, outcome distribution, abandonment, starts, completions; zero-run and all-abandoned cases; Runs from other versions excluded.
- [x] Canvas overlay shows the numbers; switching version changes them.
- [x] Outcome chart matches the overlay totals.
- [x] Non-Members get 404/forbidden on the analytics route.
- [x] Seam B: complete two Runs to different Endings → analytics shows both, with percentages.

Verification and evidence follow `docs/agents/testing.md`: cite the exact commands run; commit any artifact used as PASS evidence under `test-results/`; never include participant Responses or real run data. Use `CONTEXT.md` vocabulary. Spec: `.scratch/journeys-platform/spec.md`.

## Comments

### Note 2026-09-21 — analytics contract after ticket 18 (cycles allowed)

Published graphs may now hold cycles, so a Run's path is no longer a set of Steps (ADR-0002, `docs/adr/0002-cycles-and-back-navigation.md`): a path may repeat a Step, one entry per visit, and "current" is its last entry. For the aggregations in Seam A: abandonment counts a Run's **last** path entry only; choice take-rate counts **each traversal** of a Choice (a Choice walked twice in one Run counts twice); distinct-Steps-visited is a set over the path's values, not its length. Paths are capped at 500 entries. `backtrack_count` can be inflated by a Participant who forges the `?at` index, so treat it as noisy.

### [EXECUTION PLAN] 2026-09-22 — /implement (Claude Fable 5.1)

**Contract:** this ticket as written; no scope change. `Route: contract` added above — the ticket predates the proportional harness and carried no route line; a new Member-only surface reading Runs is contract work by `docs/agents/testing.md`'s definition. Criteria in checklist order: AC-1 Seam A aggregation with hand-computed expectations (take-rate, Ending counts, Outcome distribution, abandonment, starts, completions; zero-run and all-abandoned; other versions excluded); AC-2 the canvas overlay shows the numbers and switching version changes them; AC-3 the Outcome chart matches the overlay totals; AC-4 non-Members get 404 on the analytics route; AC-5 Seam B, two Runs to different Endings shown with percentages. Derived DoD: DoD-1 the `contract` chain green (`pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build`, then one `E2E_EVIDENCE=<the analytics spec's tests> pnpm test:e2e`; no `pnpm db:migrate`, the schema is untouched); DoD-2 every PASS artifact committed under `test-results/`, fixture journeys only, never real run data.

**Availability:** `Blocked by` 06 and 09 are both `done` on `staging`; no other claim. Branch `feat/10-analytics` from `staging` at `2f61ab0`, in a worktree at `.claude/worktrees/10-analytics/journeys` (Paul is working ticket 11 in a parallel session). The worktree carries no `.env.local`; the chain runs with throwaway values for the OAuth pairs and the auth secret, the same way ticket 16's baseline measurement did.

**Decisions resolved from the spec, `CONTEXT.md`, ADR-0002, and the 2026-09-21 comment** (the ticket leaves them open):

- Analytics live on an "Analytics" tab of the Journey page (`?tab=analytics`), beside Editor, Versions, and Responses, so the Member check and the 404 are the page's own. The Published Version is named in the address too (`&version=<id>`), chosen from a "Version" select of the Journey's versions newest first; the default is the live version, or the newest when none is live; a `version` that names none of this Journey's versions falls back to the default, so a foreign id can never read another Journey's Runs. A never-published Journey shows "Not published yet."
- Every metric is computed in one pure function, `analyticsForVersion` in `src/lib/analytics.ts`, from the version's document and its Run rows (`path`, nothing else is needed): a Run is a start; it is completed when its last path entry is an Ending of the document and abandoned otherwise, which is the spec's rule (line 196) read off the path alone; abandonment is counted on the Run's last entry only, per the 2026-09-21 comment. A Choice's take-rate counts every traversal — each consecutive pair of path entries that is one of the source Step's Choices — over every visit to the source Step (every path entry naming it), so a Step's Choices plus the Runs that stop on it account for every visit exactly once. Two Choices of one Step leading to the same Step are indistinguishable in a path, so they share the pair's number; the rate is null (shown as "—") for a Step no Run has visited. The Outcome chart groups completed Runs by the Ending's Outcome id and its label from the version's document, an untagged Ending under its own Step title (ticket 24's rule), plus an "Abandoned" group, so the bars sum to the starts and the Outcome bars sum to the Ending counts on the map (AC-3). Runs from other versions are excluded by the pure function as well as by the query, so the rule is testable by hand.
- The map is a read-only React Flow canvas of its own, `AnalyticsCanvas`, laid out by the same `layoutGraph` as the editor and drawn with the editor's own arrow geometry and box styling, moved out of `journey-canvas.tsx` into `canvas-shared.ts` for both to import. Boxes are not buttons — nothing opens — and carry the count: "N runs" on an Ending, "N abandoned" elsewhere; each arrow carries its Choice label and its take-rate and traversal count, and is drawn thicker the more it is taken. Pan, zoom, and fit-to-view as the editor. Totals (starts, completions, abandoned, completion rate) sit above the map as a row of tiles; the Outcome chart is a horizontal bar table beneath it, one hue for Outcomes and a muted bar for Abandoned, each bar directly labelled with its count and share, so the table is its own accessible reading.
- Data access is `getAnalyticsForMember` in a new `src/db/analytics.ts`: Member-checked through `getJourneyForMember` like every other read, the version constrained to the Journey, the Run rows selected by `version_id` (the index ticket 06 left for this). Nothing here ever touches `src/db/runs.ts`, whose contract is the anonymous runner's.
- `.claude/worktrees/**` is added to ESLint's ignores: a worktree's `.next/` sits under the main checkout and the top-level `.next/**` ignore does not reach it, so `pnpm lint` in the main checkout would otherwise scan this worktree's build output.

**Structure:** one session, seams in order. (1) Seam A by TDD: `src/lib/analytics.ts` and its test. (2) Data access. (3) `canvas-shared.ts` extraction, no behaviour change, the canvas spec untouched. (4) `AnalyticsCanvas`, the totals, the chart, the version select, the tab. (5) Seam B: `e2e/analytics.spec.ts` — two Participants to different Endings and a third who stops, then a second version with an untagged Ending, the select switched between them, and a non-Member's 404.

**Review:** `/code-review` (two fresh readers, standards and spec), as `contract` requires. **Red team:** not run — this reads Runs the spec already defines (lines 165–200) into a Member-only page; it changes no part of the graph model, publishing, Run semantics, auth, the schema, or the deploy path.

### [AI CODE REVIEW] 2026-09-22 — two-axis review of `2f61ab0..10e0326`, fixes in `2398ad6`

Two fresh readers (opus, `/code-review`: standards and spec) read the whole diff against the ticket, its execution plan, `CLAUDE.md`, `CONTEXT.md`, `docs/agents/testing.md`, `docs/agents/issue-tracker.md`, `docs/agents/planning.md`, spec stories 61–66a, the Analytics, Run, Back navigation, and Canvas sections, ADR-0002, and the neighbouring data-access, canvas, tab, and e2e code the change mirrors. **No blocking finding on either axis; no correctness bug found by either reader.**

**Standards** (0 hard, 1 soft, 5 smells, 2 notes): evidence and closeout not yet committed at review time (expected; committed at closeout) — resolved here; `CONTEXT.md`'s Canvas entry described only the editor's map while the change adds a second — **resolved (`2398ad6`)**, the entry now says the Analytics tab draws a published version the same way, read-only, with its runs' numbers on it; the `.claude/worktrees/**` ESLint ignore as a lasting blind spot — left, recorded in the execution plan as the reason a worktree's `.next/` would otherwise be linted from the main checkout, and Paul can drop it; Data Clumps in `selfLoopRoute` and `arrowPoints` (four positional coordinates) — **resolved (`2398ad6`)**, both take two `Point`s; Duplicated Code between the two maps' handle scaffolding and label chips — left, the extraction stopped at geometry and styling because the editor's box is a button with toolbars, a drop handle, and connect anchors and the Analytics box is none of those, so a shared box would carry props one side never uses; Speculative Generality in an unread `data-step-id` and a "missing step" branch a Published Version can never produce — **resolved (`2398ad6`)**, both removed; `figureFor` mixing `counted` with a hand-built string — left, "1 abandoned" is not a count-and-noun phrase; the analytics read running on every Journey page view — left, the tabs component renders every tab's content on the server, as the Responses list already does (follow-up below); a Run with an empty path silently not a start — **resolved (`2398ad6`)**, documented on `analyticsForVersion`; `toContainText("1 run")` passing against "11 runs" — **resolved (`2398ad6`)**, every box figure is read exactly from its own element.

**Spec** (2 partial, 3 not-asked-for, 2 looks-wrong, 0 missing; every checklist criterion confirmed covered): the ticket-27 case (a Journey whose Start is an Ending reads zero) asserted nowhere — **resolved (`2398ad6`)**, a Seam A case for exactly that document; the function's comment claimed a Run ending on a Step the document lacks is "an abandonment" while no Step's own figure counts it — **resolved (`2398ad6`)**, the comment now says it counts in the Abandoned total and on no box. Not asked for and kept, each recorded in the execution plan as a decision resolved from the spec: the ESLint ignore (above); the Abandoned and Completion rate tiles beside starts and completions, zero bars for Outcomes nothing reached (story 28's "notice an Outcome nothing reaches"), and arrows drawn thicker the more they are taken; the read on every page view (above). Looks wrong: the take-rate denominator is visits, where the spec's Analytics line says "runs that visited A" — **resolved (`2398ad6`)** by amending the spec rather than the code: with cycles (ticket 18) a path visits a Step more than once and the ticket's 2026-09-21 comment already counts every traversal, so traversals over visits is the denominator under which a Step's Choices and the Runs that stop on it account for its visits exactly once; and `ended_at` never read — the same amendment records that completion is read off the path, which is equivalent under ADR-0002 and keeps the map and the totals from disagreeing. Confirmed clean: the arithmetic in `src/lib/analytics.ts` checked by hand against every e2e assertion; `steps[step.id]` safe under `checkKeysMatchIds`; non-Members 404 on the tab and on the data-access read; no Run id or Participant id on the page; the NUL pair key a source escape, not a byte.

### [CLOSEOUT] 2026-09-22 — /implement (Claude Fable 5.1)

**PR:** https://github.com/paul-macfarlane/journeys/pull/41 (`feat/10-analytics` → `staging`). Per the tracker rule Paul set on 2026-09-21, this closeout commit carries `Status: done`; merging the PR is the acceptance that lands it on `staging`. Status log: `ready-for-agent` → `in-progress` → `ai-review` → `done`.

**Repository delivery:** `journeys`, base `staging` at `2f61ab0`, worktree `.claude/worktrees/10-analytics/journeys` (Paul worked ticket 11 in a parallel session), one implementer (this session), no delegation. Commits: `f78591d` claim and execution plan; `2addd7b` Seam A by TDD (`src/lib/analytics.ts`); `9b1f0db` the canvas geometry and color-mode hook moved into `canvas-shared.ts`, plus `src/db/analytics.ts`; `a5fd52c` the Analytics tab, the read-only map, the totals, the chart, the version select, the page wiring; `01a4400` Seam B (`e2e/analytics.spec.ts`); `10e0326` the pair key's NUL written as an escape rather than a byte; `2398ad6` review fixes; `600d3dc` the blur-saved form race; `d791827` evidence and the review record; and this closeout. Structure as planned: the five seams in order.

**Exact verified run command** (local; docker Postgres on 5436; the worktree's chain run with throwaway values for the auth secret and OAuth pairs, no `.env.local`; captured over the `600d3dc` tree, committed in `d791827` as `test-results/dod-1-commands.txt`):

```
pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build && E2E_EVIDENCE=analytics-two-runs-to-different-endings,analytics-scoped-to-a-version pnpm test:e2e
```

Exit status 0; Vitest 361 passed in 24 files; Playwright 86 passed (production build on port 3100, `retries` 0, nothing flaky). No schema change, so no `pnpm db:migrate`; no dependency or lockfile change; no deployed-target check (Paul's 2026-09-20 decision) — the post-merge smoke on staging is the human gate.

| Criterion | Verdict | Evidence |
|---|---|---|
| AC-1 Seam A: take-rate, Ending counts, Outcome distribution, abandonment, starts, completions; zero-run and all-abandoned; other versions excluded | PASS | `src/lib/analytics.test.ts` (19 cases, hand-computed: the runner Journey's three Runs, a loop walked twice, two Choices to one Step, an untagged Ending, a Start that is an Ending, a path naming a Step the version lacks, Runs of another version) in the captured Vitest run |
| AC-2 Canvas overlay shows the numbers; switching version changes them | PASS | `test-results/analytics-two-runs-to-different-endings/…png` (every box and arrow figure asserted exactly); `test-results/analytics-scoped-to-a-version/…png` (the select switched, the address carrying `version=`, a reload, a foreign id falling back to the live version) |
| AC-3 Outcome chart matches the overlay totals | PASS | Both specs read the chart's rows against the Ending boxes: Reached care 1 / Turned away 1 / Abandoned 1 against "1 run", "1 run", "1 abandoned"; version 2's untagged "Turned back" 1 (100%) against its box |
| AC-4 Non-Members get 404 on the analytics route | PASS | A second Author's `goto` of `?tab=analytics` answers 404 with no map (`analytics-two-runs-to-different-endings`); `getAnalyticsForMember` refuses a non-Member and a version of another Journey alike |
| AC-5 Seam B: two Runs to different Endings → analytics shows both, with percentages | PASS | `e2e/analytics.spec.ts`, two tests, both in the captured run; "67% · 2 times", "33% · 1 time", "50% · 1 time", "0% · 0 times" on the arrows |
| DoD-1 command chain green | PASS | `test-results/dod-1-commands.txt` |
| DoD-2 PASS artifacts committed, fixture journeys only, no real run data | PASS | `d791827`; every Run in the evidence is the spec's own walk of the fixture Journey; the page text is asserted free of Run and Participant ids |

**Deviations:** (1) `src/components/blur-saved-form.ts` changed, outside the ticket: `journey-edit-and-delete` failed once the Journey page did this ticket's extra server work — a `router.refresh()` from the title's save landed while the description was being typed, and react-hook-form's `keepDirtyValues` folded the typed text into the new defaults so the description's blur saved nothing; fixed at the cause (the server's values are the baseline, a mid-edit field is put back as an edit still to save), and the full suite then passed. (2) `.claude/worktrees/**` added to ESLint's ignores, recorded in the execution plan; Paul may drop it. (3) The spec's Analytics paragraph carries an amendment for the per-visit take-rate and path-only completion, and `CONTEXT.md`'s Canvas entry names the Analytics map. **Red team:** not run, as the execution plan records. **Follow-ups:** the analytics read (one version's Run paths, parsed document, arithmetic) runs on every Journey page view because the tabs component renders every tab's content on the server, as the Responses list already does — revisit together if a Journey ever holds thousands of Runs; the ESLint ignore and the form fix could each stand on their own if Paul prefers them out of this PR.
