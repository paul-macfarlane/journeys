# 66: Production smoke test, 2026-09-24

Status: needs-triage
Blocked by:
Owner:
Parent: `.scratch/journeys-platform/spec.md`
Priority: mixed — each finding below carries `pre-hackathon` or `post-hackathon`; the two pre-hackathon items are already handled (one as ticket 67, one as a data change on production).
Route: polish (no code; findings become tickets)

**Why:** Paul, 2026-09-24: "Let's do a smoke test of prod … document any bugs, classify them as pre/post hackathon fixes." Production is `https://journeys-ten-virid.vercel.app` at `main` `05de69d` (PR #69, which carries everything on `staging` through PR #75: tickets 60–63 and 65). Ticket 56 walked staging on the same code; this pass walks production itself, where the environment (database, AI Gateway key, OAuth, image hosts) differs.

**Where and how.** The desktop app's browser pane at its natural 646 px width, Paul signed in (the pane's profile held his session; no credentials were entered), plus `curl` for status codes, titles, and link-preview tags, and 375 px and light-scheme screenshots through the pane's viewport and colour-scheme emulation (reads only; pointer clicks under emulation do not reach the page). No screenshots are committed: the pane cannot write to disk and every finding is reproducible from its description.

## Findings

`severity — page — what happened — what was expected — disposition`

1. **pre-hackathon** — `/p/00000000-5eed-4000-8000-000000000001` and the seed Project's Journeys tab — the cases listed as Case 1, Case 3, Case 2 (the seed writes no `position`, so `created_at` broke the tie). Expected: 1, 2, 3. — **Done on production during this pass**: "Move up" on Case 2 from the Project page; the public page now reads 1, 2, 3. Post-hackathon follow-up: make `scripts/seed/journey-stories-seed.ts` set explicit positions so a re-seed does not reorder them.
2. **pre-hackathon** — the footer on every page at 375 px, and the runner's prose-width footer at the pane's 646 px — `flex-wrap` plus `justify-between` scatter whichever items wrap: at 375 the About page's footer reads "Journeys · © 2026 Paul Macfarlane · About" spread across the first row and "Guide · GitHub · Privacy Terms" spread across the second; the runner at 646 drops "Privacy Terms" alone under the mark. Expected: a wrap into two tidy rows. — **Ticket 67** (this thread).
3. **post-hackathon** — any page in a tab opened before a deploy — the first client-side navigation threw `ChunkLoadError: Failed to load chunk /_next/static/chunks/1to642o5_hzwy.js` (the old deployment's runtime `turbopack-23ahv6s7fk5iq.js` asked for a chunk the new deployment does not serve) and the page stayed on the old build with no recovery until a hard reload. Expected: the app reloads itself onto the new build, or Vercel Skew Protection keeps the old assets serving. Consider Skew Protection (Project → Settings → Advanced) or a chunk-load error boundary that reloads once.
4. **post-hackathon** — `/j/<unpublished-or-taken-down id>` (for example the demo Journey `…000021`) and `/p/<project with no live Journey>` — return HTTP 200 with "This journey isn't available" / "No journeys are available right now". Expected for crawlers and link previews: 404 (or 410 after a take-down) so the page is not indexed; the friendly copy can stay.
5. **post-hackathon** — `/robots.txt` and `/sitemap.xml` — both 404. Expected: a `robots.ts` allowing the public routes and disallowing `/projects` and `/api`, and a sitemap of the static pages.
6. **post-hackathon** (design question) — the runner's "← Back" on an Ending, then Analytics — a Run that reached the Ending "Sunlit path" (Outcome Found) read as Starts 1, Completions 0, Abandoned 1 after Back was pressed on the Ending, because completion is read off the path alone (ADR-0002, ticket 10) and Back truncates it. The count corrected itself once the Participant reached an Ending again. Expected, arguably: a Run that once reached an Ending should not become "abandoned" by backing up from it. Decide whether the Ending's Back link should exist, or whether "reached an Ending" should stick.
7. **post-hackathon** (UX) — the Step panel — adding a Choice whose target is "New step" moves the panel to the new Step with its title focused (by design, ticket 56); an Author who immediately presses "Add choice" again adds the second Choice to the new Step, not the one they were editing. Consider staying on the source Step when the Choice was added from the panel, or a clearer hand-off ("Now editing: Untitled step").
8. **post-hackathon** (note, no change) — the demo Journey "A key on the doormat" in The Allotment is "Never published" on production, so its participant link shows "This journey isn't available". The seed writes Drafts only (ticket 58). Paul's call whether to publish it before the form goes in.
9. **post-hackathon** (note, no change) — on a Step with a Back link the first Tab lands on "← Back", not Next or the first Choice, by DOM order; 56 recorded "Next or the first Choice" from the Preface, which has no Back. Fine as it is; recorded so nobody re-checks.

## No finding (walked on production, recorded so nobody re-checks)

- **Anonymous:** `/`, `/about`, `/guide`, `/privacy`, `/terms`, `/sign-in` are 200 with "· Journeys" titles; `/projects` redirects anonymous visitors to `/`; unknown routes, unknown Project and Journey ids, and the (switched-off) Author page are 404 on the ticket-60 page with the app shell and a "Page not found · Journeys" title; `og:title`, `og:description`, `og:image` (1200 × 630 PNG at the production origin), and `twitter:card` on `/`; a per-Journey `og:image` on `/j/<id>`. The seed Project page lists the three cases; Case 1 plays from the Preface, its Flickr image on "Moments of Peace in Honduras" loads (ticket 61's fix is live), Back returns to the Preface, and the page is clean at 375 px (no horizontal overflow) in both schemes. Console clean apart from finding 3.
- **Signed in (Paul's session):** the splash shows "Go to your projects" when signed in; the Projects list, the header's Projects menu (three Projects plus "All projects"), account Settings (display name, picture, Author page toggle, bio, links); New project and New journey dialogs create and land on the new page; the Step panel edits title, content, Prompt, "Required", "AI decides" (disabled with "Needs two or more choices." until the second Choice), Choices with an existing or new target, Outcome create-and-pick, Remove choice, and the "1 problem" badge for an unreachable Step, all autosaved ("Saved"); Publish showed "Published Version 1 — participants see it now." with Copy link (ticket 65); Unpublish asked "Unpublish this journey?", the participant link then read "This journey isn't available" and the Project page "No journeys are available right now"; Publish again made Version 2 and the Versions tab lists both with Restore; Analytics after a completed Run read Starts 1, Completions 1, 100 %, the Run drawn on the graph and in "Runs by outcome"; Responses lists the one response anonymously; Journey Settings offers the Theme override; Project Members lists Paul with "A project keeps its last member"; Project Settings edits title, description, preset, and accent; Delete journey and Delete project each asked "Delete permanently" and the Projects list no longer shows the test Project.
- **The judge on production:** the deciding Prompt picked "Take the sunlit path" from "I head for the sunshine…" and "Take the dark path" from "The dark one, I like a mystery." (in Preview: "The judge picked "Take the dark path" (100%) — a live run would have advanced."), each within about two seconds, so `AI_GATEWAY_API_KEY` and the model are live on production.
- **Runs written to production:** one abandoned Run on Case 1 (stopped on its second Step) from this pass, on top of the six from ticket 56; the two Runs on the test Journey went with it when it was deleted. No screenshot or record here carries a real participant Response.

Acceptance criteria: triage items 3–9 into tickets or `wontfix` lines here; items 1 and 2 are handled as stated.

Spec: `.scratch/journeys-platform/spec.md`. Origin: Paul, 2026-09-24, "smoke test of prod".
