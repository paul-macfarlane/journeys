# 19: Canvas quality of life

Status: in-progress
Blocked by: 16
Owner: Atlas orchestrator (Claude Fable 5.1), session of Paul Macfarlane, claimed 2026-09-21
Parent: `.scratch/journeys-platform/spec.md`
Priority: third of the graph tidy-up series (18 → 16 → 19 → 21 → 17).

**What to build:** the smaller editor conveniences a writer who lives on the canvas will reach for.

- **Find a step.** A "Find step" field above the canvas (also opened with Cmd/Ctrl+K on the Journey page) filters Step titles as you type; choosing one selects it and centers the map on it.
- **Duplicate step.** "Duplicate" in the box toolbar and the panel footer copies a Step's title (suffixed " copy"), content, Prompt, and Outcome, with no Choices, places it beside the original, and opens it.
- **Content peek.** Hovering a box (or focusing it) shows the first ~140 characters of its content as plain text in a tooltip so the map can be skimmed without opening every Step.
- **Selected step stays in view.** The panel column is sticky so the selected Step's fields stay beside the map while the page scrolls.
- **Map keyboard basics.** With a box focused: Enter opens it (already), arrow keys move focus to the nearest box in that direction, Escape returns focus to the canvas.
- **Zoom to selection / fit.** A "Fit" control already exists; add "Zoom to step" in the box toolbar.

Not included, recorded as decisions for Paul: **undo/redo** for structural edits (the spec excludes undo; an in-memory history stack of the Draft document with Cmd/Ctrl+Z is a medium change worth its own ticket if wanted) and **collapsing or removing the step list** beyond ticket 16's disclosure.

- [ ] Typing part of a title in "Find step" and choosing the match selects that Step and brings its box into the viewport (case-3, a Step at the bottom of the map).
- [ ] "Duplicate" creates a Step with the same content and Outcome, no Choices, title suffixed " copy", opened in the panel.
- [ ] Hovering a box shows its content's opening text.
- [ ] Arrow keys move focus between boxes; Enter opens the focused one.
- [ ] Seam B: find, duplicate, and edit through the map; screenshots under `test-results/`.

Verification and evidence follow `docs/agents/testing.md`: cite the exact commands run; commit any artifact used as PASS evidence under `test-results/`; never include participant Responses or real run data. Use `CONTEXT.md` vocabulary. Spec: `.scratch/journeys-platform/spec.md`. Origin: Paul's usage feedback on PR #19, 2026-09-21.

## Comments

### Note from ticket 16's review (2026-09-21)

A self-loop Choice (a Step whose Choice targets itself) can be drawn by dragging on the canvas since ticket 16, and its arrow is routed straight through its own box: dagre keeps the loop in the box's rank and runs it out to the right and back (`layout.test.ts` pins the shape). A hand-built side route in the `choice` edge is this ticket's territory.

### Scope addition (Paul, 2026-09-21): remove the step list

Once "Find step" and the map keyboard basics land, remove the step list under the canvas (`StepList`, its "Steps" disclosure, and the `list "Steps"` reads in `draft.spec.ts`, `step-editing.spec.ts`, `publish.spec.ts`, `canvas.spec.ts`). The find-step palette lists every Step in map order when the query is empty, which is what the list was for; arrow keys between boxes replace it for keyboard navigation. The Outcomes editor beneath the canvas stays. The "Selected step stays in view" bullet's sticky panel is superseded if a slidable panel is decided before this ticket runs.

### [EXECUTION PLAN] 2026-09-21 — Atlas orchestrator

**Contract:** this ticket as it stands: the six "What to build" bullets, the five acceptance criteria (AC-1..AC-5 in checklist order), the scope addition "remove the step list" (S-1), and the ticket-16 review note assigning the self-loop side route here (N-1). Two readings resolved from the ticket's own text, not changes to it: (a) the "Selected step stays in view" sticky panel is superseded — ticket 21 (a slidable panel) was decided on 2026-09-21 before this ticket ran, exactly the case the scope-addition comment names — so no sticky column is built; (b) "places it beside the original" for Duplicate cannot be literal before ticket 17: the map is laid out by dagre from the document alone (ADR-0001, `CONTEXT.md` "never by hand"), so a copy with no incoming Choice lands where dagre puts an unconnected Step and the map fits to show it; recorded as an approved deviation, not a question, because the only alternative is a stored position, which is ticket 17's decision. Derived DoD from the footer, `docs/agents/testing.md`, and `CLAUDE.md`: DoD-1 verified run command green; DoD-2 every PASS artifact committed under `test-results/`, fixture journeys only, no participant data. No dependency, schema, or migration change is expected (no new package: the find field is a plain combobox on the existing `Input`), so no lockfile gate and no migrate step. Run surface: local + deployed; per Paul's 2026-09-20 decision there is no post-merge staging smoke gate. No human gates at plan time. The spec never mandates a step list, so S-1 needs no `[SCOPE CHANGE]` on the spec; README's editor paragraph is updated instead.

**Availability:** `Blocked by` 16 is `done` on `staging` (`33baeb4`, PR #23 merged); Paul's priority order (19 → 21 → 17) names 19 next.

**Repository delivery:** `journeys`, base `staging` @ `33baeb4`, branch `feat/19-canvas-quality-of-life`, direct checkout, no worktrees. Deliverables run **sequentially**: every one verifies through the one e2e port (3100) and the one `journeys_e2e` database, and the predicted file overlaps are real — all three rewrite `src/components/journeys/journey-canvas.tsx` and `e2e/canvas.spec.ts`; D1 and D2 both touch `draft-editor.tsx`. Re-checked at closeout against the real diffs.

**Resolved technical decisions (execution, not contract changes):**

- **Find step (D1).** `src/components/journeys/find-step.tsx`: a combobox built on the app's `Input` (no new dependency) — `role="combobox"`, accessible name "Find step" (a visually hidden `<label>`; placeholder "Find step…"), `aria-autocomplete="list"`, `aria-expanded`, `aria-controls` naming the listbox, `aria-activedescendant` naming the active option. The listbox (`role="listbox"`, `aria-label="Steps"`) opens on focus, click, ArrowDown, or typing; it lists every Step as an `option` in map order (`mapOrder(layout)`, already computed by the editor) when the query is empty, and the Steps whose `stepName` contains the query case-insensitively when it is not. Each option's accessible name is the Step's name alone (`aria-label`); the Start and Ending badges the old list carried are decoration inside it, so the old list assertions survive with `option` in place of `listitem`. ArrowDown/ArrowUp move the active option (wrapping), Enter chooses it, a pointer click chooses it (`onMouseDown` prevented so blur does not close the list first), Escape closes the list and a second Escape clears the query; blur closes it. No match: an empty listbox and a "No steps match" line beside it. Choosing calls `onSelectStep(stepId, { center: true })`, closes the list, clears the query, and leaves focus in the field. The field sits at the top of the canvas column, directly above the map, `max-w-sm`; the listbox is absolutely positioned beneath it over the map (`z-20`, `max-h-80`, scrollable). **Cmd/Ctrl+K:** a `keydown` listener on `window` mounted by `DraftEditor` (the Journey page is the only page that mounts it): Meta/Ctrl + `k` (not already `defaultPrevented`) prevents the default and focuses the field, selecting its text.
- **Center on a found Step (D1).** `SelectStepOptions` gains `center?: boolean`. The editor's `locateRequest` becomes `locate: { request: number; center: boolean }` (or an equivalent second prop); the canvas locate effect, when `center` is set, skips its on-map check and always runs `fitView({ nodes: [{ id }], maxZoom: 1, duration: 200 })` — the same move ticket 16's locate makes for an off-map box, so a Step found at fit-all zoom on case-3 is shown readable and centered. Every other opening keeps ticket 16's "leave an on-map box alone" rule.
- **Step list removed (D1).** `src/components/journeys/step-list.tsx` is deleted with its `DraftEditor` mount; the "Steps" `h2` and its summary stay (specs read the heading). `OutcomeList` takes the full width beneath the map (the `md:grid-cols-2` wrapper goes). `openStepList` in `e2e/setup/authoring.ts` becomes `openFindStep(page)` (click the "Find step" combobox, expect the "Steps" listbox visible); every `list "Steps"` read in `draft.spec.ts`, `step-editing.spec.ts`, `publish.spec.ts`, and `canvas.spec.ts` becomes a `listbox "Steps"` / `option` read; `canvas-step-list-follows-map` becomes `canvas-find-step-lists-map-order` (the empty-query listbox's option names equal the boxes sorted top-to-bottom then left-to-right — ticket 16's AC-8 in its new home); `canvas-locate-on-map`'s list half chooses its Step from the listbox instead. README's "pick a step from the list" and "the step list under the map follows the map's own order behind a 'Steps' disclosure" are rewritten for the find field.
- **Duplicate (D2).** `duplicateStep(document, stepId): { document; stepId }` in `src/lib/graph/edit.ts`: a new Step (`crypto.randomUUID()`) whose `title` is the original's followed by `" copy"` (the original trimmed to keep the whole within the schema's 200 characters), `content` a structural copy (`structuredClone`), `prompt` and `outcomeId` copied, `choices: []`, `position: null`; an unknown Step returns the document unchanged with `stepId: ""`. Seam A cases in `edit.test.ts`. The editor's `duplicate(stepId)` applies it and opens the copy with `focusTitle` (the existing fit-on-new-box brings it into view). "Duplicate" is a `Button` in the box toolbar (after "Add next step") and in the panel footer (beside "Make this the start").
- **Zoom to step (D2).** A "Zoom to step" `Button` in the box toolbar, calling `fitView({ nodes: [{ id }], maxZoom: 1.5, duration: 200 })` through `useReactFlow` inside the canvas; no editor plumbing.
- **Content peek (D3).** `contentPreview(content, limit = 140): string` in `src/lib/graph/content.ts` (pure): the text of every paragraph, heading, and list item in document order joined by single spaces, whitespace collapsed, images contributing nothing; longer than `limit` is cut at `limit` and given a trailing `…`. Seam A cases in `content.test.ts`. On the map, each Step box renders a `role="tooltip"` element (a second `NodeToolbar` at `Position.Bottom`, so it neither scales with zoom nor is clipped by the box) visible while the box is hovered or its button focused and hidden otherwise, with the box's problem messages (one per line) first and then the preview, or "No content yet" when the preview is empty; the button's `aria-describedby` names it and its `title` attribute goes (a `title` never showed on focus). No spec reads the old `title`.
- **Map keyboard basics (D3).** `onKeyDown` on the Step box button (placeholders included, both are boxes): ArrowUp/Down/Left/Right prevent the default and move focus to the nearest box in that direction — candidates are boxes whose centre lies strictly on that side of the focused box's centre; score = distance along the axis + 2 × the perpendicular offset; the lowest wins; none means focus stays. The newly focused box, if off the map, is brought on with the same `fitView` move locate makes; the panel's selection does not change (Enter opens, as now). Escape moves focus to the map's own focusable container (the React Flow pane) and clears any arrow selection. Neighbour lookup runs off the `nodes` the canvas already holds, through the actions context.
- **Self-loop side route (N-1, D3).** In `ChoiceEdge`, when `source === target` the dagre points are ignored and the path is hand-built in flow coordinates: down from the source anchor by 24, right to 32 past the box's right edge (`targetX + NODE_WIDTH / 2 + 32`), up to 24 above the box's top, left to the target anchor's x, and down into it; smoothed as every other arrow, label at its midpoint. `layout.ts` and its self-loop test are unchanged.

**Deliverables (all `atlas-worker`, direct checkout on `feat/19-canvas-quality-of-life`, in this order):**
- **D1 — find step and the step list's removal** (opus): `find-step.tsx` (new), `draft-editor.tsx`, `journey-canvas.tsx` (center mode), `editor-shared.ts`, `step-list.tsx` (deleted), `e2e/setup/authoring.ts`, `e2e/canvas.spec.ts` (AC-1 `canvas-find-step`, `canvas-find-step-lists-map-order`, the `canvas-locate-on-map` rewrite), `draft.spec.ts`, `step-editing.spec.ts`, `publish.spec.ts`, README.
- **D2 — duplicate and zoom to step** (sonnet): `edit.ts` + `edit.test.ts`, `step-panel.tsx`, `journey-canvas.tsx` (two toolbar buttons), `draft-editor.tsx`, `e2e/canvas.spec.ts` (AC-2 `canvas-duplicate-step`).
- **D3 — content peek, keyboard basics, self-loop route, Seam B** (opus): `content.ts` + `content.test.ts`, `journey-canvas.tsx`, `e2e/canvas.spec.ts` (AC-3 `canvas-content-peek`, AC-4 `canvas-keyboard-navigation`, N-1 inside `canvas-connect-and-retarget-by-dragging`, AC-5 `canvas-find-duplicate-and-edit` with its recording).

**Verification map (evidence committed under `test-results/`; the root is cleared once, immediately before the aggregate capture):**

| Criterion | Command / action | Surface & real deps | Expected | Evidence | Earliest | Invalidated by |
|---|---|---|---|---|---|---|
| AC-1 | `pnpm test:e2e` test `canvas-find-step` (case-3) | local; docker Postgres `journeys_e2e`; production server on 3100 | Cmd/Ctrl+K focuses "Find step"; typing part of the title of the bottom-most box (lowest `y`, read off the map) lists it; choosing it opens that Step in the panel and its box is fully inside the canvas frame | `test-results/canvas-find-step/….png`, `test-results/dod-1-e2e.txt` | after D1 | changes under `src/`, `e2e/` |
| AC-2 | test `canvas-duplicate-step`; Seam A `edit.test.ts` (`duplicateStep`) | as AC-1 | "Duplicate" in the toolbar on an Ending with content and an Outcome opens "<title> copy" with the title focused; the panel shows the same content and Outcome and no Choices; the `draft` row's new Step has `choices: []`, equal `content`, `prompt`, `outcomeId`; the panel footer's "Duplicate" makes a second copy | `test-results/canvas-duplicate-step/….png`, `dod-1-e2e.txt`, `dod-1-commands.txt` | after D2 | as AC-1 |
| AC-3 | test `canvas-content-peek`; Seam A `content.test.ts` (`contentPreview`) | as AC-1 | hovering a box shows a `tooltip` whose text starts with the Step's opening words; focusing the box shows it too; a Step with no content reads "No content yet" | `test-results/canvas-content-peek/….png`, `dod-1-e2e.txt`, `dod-1-commands.txt` | after D3 | as AC-1 |
| AC-4 | test `canvas-keyboard-navigation` | as AC-1 | on a Start with two children: ArrowDown from the Start box focuses a child; ArrowRight/ArrowLeft move between the siblings; ArrowUp returns to the Start; Enter on a focused box opens it in the panel; Escape leaves focus on the map's container | `test-results/canvas-keyboard-navigation/….png`, `dod-1-e2e.txt` | after D3 | as AC-1 |
| AC-5 | test `canvas-find-duplicate-and-edit` (case-3) | as AC-1 | a Step found through "Find step", duplicated from the toolbar, renamed and given content in the panel, connected from the original by dragging; the `draft` row holds the copy with its new title and the Choice; screenshot and recording | `test-results/canvas-find-duplicate-and-edit/….png`, `….webm`, `dod-1-e2e.txt` | after D3 | as AC-1 |
| S-1 | test `canvas-find-step-lists-map-order` (case-3); `grep -rn "StepList\|step-list" src e2e`; `grep -n 'name: "Steps"' e2e/*.spec.ts` | as AC-1; local | the empty-query listbox's option names equal the boxes sorted top-to-bottom then left-to-right (36); no `StepList` or `step-list` reference under `src/` or `e2e/`; no `list "Steps"` read remains (only the heading) | `test-results/canvas-find-step-lists-map-order/….png`, `test-results/s-1-step-list-removed.txt`, `dod-1-e2e.txt` | after D1 | as AC-1 |
| N-1 | test `canvas-connect-and-retarget-by-dragging` (self-loop case) | as AC-1 | dragging the connect dot onto its own box adds a Choice targeting itself; the arrow's path midpoint lies outside the box's own rect | `test-results/canvas-connect-and-retarget-by-dragging/….png`, `dod-1-e2e.txt` | after D3 | as AC-1 |
| DoD-1 | `DATABASE_URL=postgresql://postgres:postgres@localhost:5436/journeys pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build && DATABASE_URL=postgresql://postgres:postgres@localhost:5436/journeys pnpm test:e2e` | local; docker Postgres | all exit 0; no test retried | `test-results/dod-1-commands.txt`, `test-results/dod-1-e2e.txt` | after D3 | any source, config, or test change |
| DoD-2 | evidence review at closeout | local | every PASS artifact committed; fixture journeys only | this ticket's `[CLOSEOUT]` | closeout | — |
